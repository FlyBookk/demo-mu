# ngrok 远程访问环境配置
# 使用方式：npm run dev -- --mode ngrok
#
# 使用步骤：
# 1. 启动后端服务（端口 8080）
# 2. 启动 ngrok：ngrok start --all（或 ngrok http 8080）
# 3. 将 ngrok 显示的后端地址填入下方
# 4. 启动前端：npm run dev -- --mode ngrok
# 5. 启动前端 ngrok：ngrok http 5173
# 6. 分享前端 ngrok 地址给其他人访问

# 后端 API 地址（替换为你的后端 ngrok 地址）
# 示例：https://abc123.ngrok.io 或 https://abc123.ngrok-free.app
VITE_API_BASE_URL=https://unreproached-krystyna-desperately.ngrok-free.dev
