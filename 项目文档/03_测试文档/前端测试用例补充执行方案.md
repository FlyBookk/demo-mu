# 前端测试用例补充方案 - 执行指南

**执行期限**: 2026年1月20日 - 2026年2月2日(14天)
**目标**: 将测试用例覆盖率从58%提升到85%+

---

## 第一阶段：紧急补充(第1-2周)

### 任务1: 补充字段映射画布交互用例 (4条, 2天完成)

**文件位置**: 在测试用例文档中插入到 TC-MAP-008 之后

```markdown
#### TC-MAP-009 拖拽连线预览效果

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-MAP-009 |
| **功能模块** | 字段映射 - 拖拽反馈 |
| **用例标题** | 验证拖拽过程中连线预览显示 |
| **优先级** | P1 |
| **前置条件** | 进入字段映射画布 |
| **测试步骤** | 1. 鼠标按住左侧CSV字段"order_id"<br>2. 向右侧系统字段"订单号"方向拖拽<br>3. 拖拽过程中移动鼠标<br>4. 将字段释放到"订单号"上 |
| **预期结果** | 1. 拖拽开始时显示虚线连接预览<br>2. 预览线随鼠标实时跟踪<br>3. 预览线颜色为灰色/虚线<br>4. 释放时连线变为实线(绿色)<br>5. 映射完成后源字段和目标字段均显示已映射状态<br>6. 连线显示平滑，无闪烁 |

---

#### TC-MAP-010 映射冲突提示和替换

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-MAP-010 |
| **功能模块** | 字段映射 - 冲突处理 |
| **用例标题** | 验证映射到已占用字段时的冲突处理 |
| **优先级** | P1 |
| **前置条件** | 1. 进入字段映射画布<br>2. 已建立"date/time"到"交易日期时间"的映射 |
| **测试步骤** | 1. 尝试将另一个字段"transaction_date"拖拽到"交易日期时间"<br>2. 观察系统提示<br>3. 点击"替换原映射"<br>4. 观察映射结果 |
| **预期结果** | 1. 拖拽到已占用字段时显示冲突弹窗<br>2. 弹窗显示"该字段已被映射，是否替换?"<br>3. 提供"替换原映射"和"取消"两个选项<br>4. 选择替换后原映射解除<br>5. 新映射建立<br>6. "date/time"字段变为未映射状态<br>7. "transaction_date"和"交易日期时间"显示已映射 |

---

#### TC-MAP-014 大数据字段列表性能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-MAP-014 |
| **功能模块** | 字段映射 - 性能 |
| **用例标题** | 验证大字段列表(100+)滚动和拖拽性能 |
| **优先级** | P1 |
| **前置条件** | 上传包含120个字段的CSV文件 |
| **测试步骤** | 1. 进入字段映射画布<br>2. 在左侧CSV字段列表中快速向下滚动<br>3. 滚动到底部并快速向上滚动<br>4. 在滚动过程中进行拖拽映射<br>5. 使用Page Down/Page Up快速翻页<br>6. 使用Ctrl+Home/End快速到达首尾 |
| **预期结果** | 1. 滚动流畅，无明显卡顿<br>2. 滚动帧率(FPS)>=30<br>3. 拖拽操作不受滚动状态影响<br>4. 连线计算准确，无延迟<br>5. 页面响应时间<200ms<br>6. 内存占用不超过100MB |

---

#### TC-MAP-015 必填字段红色警告显示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-MAP-015 |
| **功能模块** | 字段映射 - 必填提示 |
| **用例标题** | 验证必填字段未映射时的红色警告显示 |
| **优先级** | P1 |
| **前置条件** | 进入字段映射画布 |
| **测试步骤** | 1. 不映射必填字段<br>2. 观察右侧系统字段列表中必填字段的显示<br>3. 检查顶部统计区域<br>4. 尝试点击"开始导入"<br>5. 映射一个必填字段后重新观察 |
| **预期结果** | 1. 必填字段在右侧显示红色标记(如红色*或红色背景)<br>2. 未映射的必填字段显示为红色/橙色<br>3. 顶部显示黄色/红色警告: "还有X个必填字段未映射"<br>4. 导入按钮显示禁用或点击后弹出错误提示<br>5. 映射必填字段后警告消失<br>6. 导入按钮变为可用 |
```

**执行说明**:
- 使用现有前端组件FieldMappingCanvas验证
- 测试工具：Chrome DevTools + 手工操作
- 预期用时：4小时

---

### 任务2: 补充文件上传交互用例 (4条, 2天完成)

**文件位置**: 在测试用例文档中插入到 TC-IMP-010 之后

```markdown
#### TC-IMP-011 拖拽文件到上传区

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-IMP-011 |
| **功能模块** | 销售数据 - 拖拽上传 |
| **用例标题** | 验证拖拽文件到上传区的交互 |
| **优先级** | P1 |
| **前置条件** | 处于销售数据导入页面，准备好CSV文件 |
| **测试步骤** | 1. 打开文件管理器，准备CSV文件<br>2. 从文件管理器将CSV文件拖拽到上传区<br>3. 拖拽过程中观察上传区反馈<br>4. 释放鼠标 |
| **预期结果** | 1. 拖拽过程中上传区高亮(背景色改变)<br>2. 显示"释放以上传文件"的提示文字<br>3. 释放后文件自动开始上传或进入预览<br>4. 显示上传进度<br>5. 上传完成后显示成功提示 |

---

#### TC-IMP-012 上传进度条显示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-IMP-012 |
| **功能模块** | 销售数据 - 上传进度 |
| **用例标题** | 验证文件上传进度显示的准确性 |
| **优先级** | P1 |
| **前置条件** | 上传一个5-10MB的CSV文件 |
| **测试步骤** | 1. 点击选择文件或拖拽文件到上传区<br>2. 开始上传<br>3. 观察进度条显示<br>4. 记录进度百分比和显示的上传速度<br>5. 等待上传完成 |
| **预期结果** | 1. 显示百分比进度条(0-100%)<br>2. 进度条平滑上升，无跳跃<br>3. 显示上传速度(如 "2.5 MB/s")<br>4. 显示已上传大小和总大小(如 "2.5MB / 10MB")<br>5. 显示剩余时间(如 "剩余3秒")<br>6. 100%时进度条显示完成，文件上传成功 |

---

#### TC-IMP-013 上传暂停和取消

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-IMP-013 |
| **功能模块** | 销售数据 - 上传控制 |
| **用例标题** | 验证上传暂停和取消功能 |
| **优先级** | P1 |
| **前置条件** | 上传一个较大的文件(>10MB) |
| **测试步骤** | 1. 开始上传文件<br>2. 等待进度到50%<br>3. 点击"暂停"按钮<br>4. 等待3秒<br>5. 点击"继续"按钮<br>6. 观察上传继续<br>7. 再次等待进度到75%<br>8. 点击"取消"按钮 |
| **预期结果** | 1. 点击暂停后进度条停止<br>2. 暂停按钮变为"继续"按钮<br>3. 暂停期间网络请求停止<br>4. 点击继续后上传从暂停处继续<br>5. 点击取消后上传停止<br>6. 显示"取消上传"提示<br>7. 已上传的部分文件被清空<br>8. 上传区恢复初始状态 |

---

#### TC-IMP-014 上传失败重试

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-IMP-014 |
| **功能模块** | 销售数据 - 上传错误处理 |
| **用例标题** | 验证上传失败时的重试机制 |
| **优先级** | P1 |
| **前置条件** | 能够模拟网络中断(使用浏览器开发工具) |
| **测试步骤** | 1. 打开Chrome DevTools > Network<br>2. 开始上传文件<br>3. 当进度到50%时，在DevTools中throttle连接(Offline)<br>4. 等待上传失败<br>5. 观察错误提示<br>6. 恢复网络连接<br>7. 点击"重试"按钮 |
| **预期结果** | 1. 网络中断后上传停止<br>2. 显示清晰的错误提示(如"网络连接失败，请检查网络")<br>3. 提供"重试"按钮<br>4. 恢复网络后点击重试<br>5. 上传从暂停处继续或重新开始<br>6. 最终上传成功<br>7. 不显示技术性错误信息 |
```

**执行说明**:
- 实现文件上传组件的这些交互
- 测试工具：真实文件+Network模拟
- 预期用时：6小时

---

### 任务3: 补充加载状态测试用例 (3条, 1.5天完成)

**文件位置**: 在 P2 一般用例中新增

```markdown
#### TC-LOAD-001 页面初始加载骨架屏

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-LOAD-001 |
| **功能模块** | 通用 - 加载状态 |
| **用例标题** | 验证页面初始加载时的骨架屏显示 |
| **优先级** | P1 |
| **前置条件** | 配置网络限速(Slow 3G或更慢) |
| **测试步骤** | 1. 打开Chrome DevTools<br>2. 设置网络为"Slow 3G"<br>3. 刷新销售数据列表页面<br>4. 观察加载过程(2-5秒)<br>5. 等待页面完全加载 |
| **预期结果** | 1. 立即显示骨架屏(灰色占位符)<br>2. 骨架屏布局与实际内容一致<br>3. 骨架屏显示表格头、多行占位符<br>4. 数据加载完成后平滑替换为真实内容<br>5. 过渡效果流畅，无闪烁<br>6. 骨架屏显示时间 < 100ms |

---

#### TC-LOAD-002 异步数据加载指示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-LOAD-002 |
| **功能模块** | 通用 - 加载指示 |
| **用例标题** | 验证异步操作中加载指示器显示 |
| **优先级** | P1 |
| **前置条件** | 生成大数据量报表(10万+行数据) |
| **测试步骤** | 1. 进入汇总报表页面<br>2. 选择大时间范围(整年数据)<br>3. 点击"生成报表"<br>4. 观察生成过程(预期5-10秒) |
| **预期结果** | 1. 立即显示加载Spinner或进度条<br>2. 显示"正在生成报表，请稍候..."文字提示<br>3. Spinner动画流畅旋转<br>4. 5秒后显示"正在生成，已完成50%"<br>5. 进度指示器准确<br>6. 生成完成后加载指示消失<br>7. 显示成功提示<br>8. 报表数据完整显示 |

---

#### TC-LOAD-003 表格虚拟滚动加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-LOAD-003 |
| **功能模块** | 通用 - 虚拟滚动 |
| **用例标题** | 验证大数据表格虚拟滚动性能 |
| **优先级** | P1 |
| **前置条件** | 表格加载10万行数据 |
| **测试步骤** | 1. 打开销售数据列表<br>2. 确保数据量>10万行<br>3. 快速滚动到表格底部<br>4. 观察滚动流畅度<br>5. 使用Page Down连续翻页<br>6. 打开浏览器Performance工具<br>7. 快速滚动，记录FPS |
| **预期结果** | 1. 页面初始加载快速(2秒内)<br>2. 虽然10万行数据，但DOM节点<200<br>3. 滚动FPS >= 30<br>4. 滚动过程中无明显卡顿<br>5. 快速滚动时新数据快速加载<br>6. 内存占用<100MB<br>7. 滚动条位置准确反映当前位置 |
```

**执行说明**:
- 使用Chrome DevTools进行网络限速
- 测试工具：Chrome DevTools Network + Performance
- 预期用时：4小时

---

### 任务4: 补充错误和空状态用例 (4条, 1.5天完成)

**文件位置**: 在 TC-ERR-002 之后新增

```markdown
#### TC-EMPTY-001 数据列表空状态

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-EMPTY-001 |
| **功能模块** | 通用 - 空状态 |
| **用例标题** | 验证无数据时的空状态显示 |
| **优先级** | P1 |
| **前置条件** | 销售数据列表为空(无数据导入) |
| **测试步骤** | 1. 进入销售数据列表<br>2. 观察页面显示<br>3. 查看是否有操作引导 |
| **预期结果** | 1. 显示空状态图标/插画<br>2. 显示"暂无销售数据"提示文字<br>3. 提供"导入销售数据"蓝色按钮<br>4. 按钮可点击，跳转到导入页面<br>5. 提供帮助文档链接<br>6. 整体视觉友好，无错误提示 |

---

#### TC-ERROR-001 表单字段校验错误

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-ERROR-001 |
| **功能模块** | 通用 - 表单校验 |
| **用例标题** | 验证必填字段为空时的错误提示 |
| **优先级** | P1 |
| **前置条件** | 进入广告数据-新增页面 |
| **测试步骤** | 1. 留空"月份"字段<br>2. 留空"金额"字段<br>3. 点击"保存"按钮<br>4. 观察错误提示 |
| **预期结果** | 1. 页面不提交<br>2. 空字段显示红色边框<br>3. 每个字段下方显示"该字段不能为空"(红色文字)<br>4. 文字大小>=12px，清晰可读<br>5. 错误提示位置紧贴输入框下方<br>6. 页面顶部不显示通用错误提示(错误集中在字段) |

---

#### TC-ERROR-002 业务规则校验错误

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-ERROR-002 |
| **功能模块** | 通用 - 业务校验 |
| **用例标题** | 验证业务规则违反时的错误处理 |
| **优先级** | P1 |
| **前置条件** | 已导入订单号"123-456-789"的销售数据 |
| **测试步骤** | 1. 再次导入包含相同订单号"123-456-789"的CSV<br>2. 在导入对话框中观察检测结果<br>3. 观察是否有选项处理冲突 |
| **预期结果** | 1. 系统检测到重复数据<br>2. 显示"发现1条重复数据"(黄色/橙色警告)<br>3. 显示重复的订单号和行号<br>4. 提供"跳过重复"或"覆盖"选项<br>5. 用户可选择处理方式<br>6. 不自动导入，需用户确认<br>7. 提供"查看详情"链接查看冲突详情 |

---

#### TC-ERROR-003 网络错误处理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-ERROR-003 |
| **功能模块** | 通用 - 网络错误 |
| **用例标题** | 验证网络断开时的错误提示和恢复 |
| **优先级** | P1 |
| **前置条件** | 系统正常运行 |
| **测试步骤** | 1. 打开Chrome DevTools<br>2. 设置Network为"Offline"<br>3. 尝试点击任意操作(如查看列表)<br>4. 观察错误提示<br>5. 设置Network回到"Online"<br>6. 点击"重试"按钮 |
| **预期结果** | 1. 立即显示"网络连接失败"错误提示<br>2. 提示位置在页面顶部(notification风格)<br>3. 提示颜色为红色/错误色<br>4. 提示文字清晰：如"网络连接失败，请检查你的网络连接"<br>5. 提供"重试"和"关闭"按钮<br>6. 网络恢复后点击重试成功<br>7. 不显示技术错误信息(如HTTP 500) |
```

**执行说明**:
- 确保有对应的前端页面或组件实现这些状态
- 使用Chrome DevTools模拟离线状态
- 预期用时：4小时

---

### 任务5: 补充表格交互用例 (3条, 1.5天完成)

**文件位置**: 在 TC-UI-003 之后新增

```markdown
#### TC-TABLE-001 行复选框选择

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-TABLE-001 |
| **功能模块** | 通用 - 行选择 |
| **用例标题** | 验证表格行复选框的选择功能 |
| **优先级** | P1 |
| **前置条件** | 销售数据列表有数据(>3条) |
| **测试步骤** | 1. 点击第1行的复选框<br>2. 观察行的选中状态<br>3. 点击第2行的复选框<br>4. 点击表头的复选框(全选)<br>5. 再次点击表头(取消全选) |
| **预期结果** | 1. 单行选中后，该行背景色改变(通常为浅蓝色)<br>2. 复选框显示对勾标记<br>3. 多行选中后，显示"已选择X行"<br>4. 表头复选框点击后所有行被选中<br>5. 所有行复选框均显示对勾<br>6. 再次点击表头复选框后所有行取消选中<br>7. 选中状态切换流畅 |

---

#### TC-TABLE-002 行内操作按钮

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-TABLE-002 |
| **功能模块** | 通用 - 行内操作 |
| **用例标题** | 验证行内操作按钮的显示和交互 |
| **优先级** | P1 |
| **前置条件** | 销售数据列表有数据 |
| **测试步骤** | 1. 鼠标移至表格某一行<br>2. 观察操作按钮显示<br>3. 保持鼠标在行上1秒<br>4. 移出该行<br>5. 观察按钮消失<br>6. 点击"编辑"按钮 |
| **预期结果** | 1. 鼠标进入行时，该行高亮显示<br>2. 右侧显示"编辑"和"删除"按钮<br>3. 按钮自动显示，无需额外点击<br>4. 按钮显示位置不导致行内容错位<br>5. 鼠标移出行后按钮消失<br>6. 点击编辑后弹出编辑对话框<br>7. 按钮颜色清晰可见(对比度>4.5:1) |

---

#### TC-TABLE-003 列排序功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-TABLE-003 |
| **功能模块** | 通用 - 排序 |
| **用例标题** | 验证表格列排序功能 |
| **优先级** | P1 |
| **前置条件** | 销售数据列表有日期和金额数据 |
| **测试步骤** | 1. 观察表头，找到"日期"列<br>2. 点击"日期"列头<br>3. 观察数据排列顺序<br>4. 再次点击"日期"列头<br>5. 观察排序方向反转<br>6. 点击其他列头进行排序 |
| **预期结果** | 1. 点击列头时显示排序箭头(↑ 或 ↓)<br>2. 数据按升序排列(最旧的日期在前)<br>3. 再次点击后按降序排列<br>4. 当前排序列显示箭头和高亮<br>5. 其他列不显示排序箭头<br>6. 排序响应快速(<100ms)<br>7. 排序后所有行数据正确 |
```

**执行说明**:
- 验证已有的表格组件(vxe-table)功能
- 测试工具：手工操作+浏览器审查元素
- 预期用时：4小时

---

### 任务6: 修改现有用例添加交互验证 (2天完成)

**修改清单**:

1. **TC-IMP-006** - 添加进度显示验证
2. **TC-MAP-004** - 添加修正流程
3. **TC-DUP-001** - 添加详情和选项
4. **TC-SUM-010** - 添加交互验证
5. **TC-AUTH-004** - 添加详细步骤

**修改示例** (以TC-IMP-006为例)：

```markdown
#### TC-IMP-006 CSV大文件导入(性能)修订版

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-IMP-006 |
| **功能模块** | 销售数据 - CSV导入 |
| **用例标题** | 验证大文件导入性能和进度显示 |
| **优先级** | P0 |
| **前置条件** | 准备10万行的CSV测试文件(约50MB) |
| **测试步骤** | 1. 打开Chrome DevTools > Performance<br>2. 上传10万行CSV文件<br>3. 观察上传进度条显示<br>4. 记录每30秒的进度百分比<br>5. 执行导入操作<br>6. 观察导入进度显示<br>7. 记录导入完成时间<br>8. 检查是否有内存警告<br>9. 等待导入完成，查看成功提示 |
| **预期结果** | 1. 文件上传进度显示准确，误差<5%<br>2. 进度条平滑更新，每100-500ms更新一次<br>3. 显示上传速度和剩余时间<br>4. 导入启动后立即显示进度Spinner<br>5. 导入进度显示准确(如"已导入50000/100000行")<br>6. 导入完成时间<5分钟<br>7. 内存占用峰值<500MB<br>8. 页面无卡顿，滚动FPS>=30<br>9. 导入完成显示成功提示和导入摘要 |
```

---

## 第二阶段：优化补充(第3-4周)

### 任务7: 响应式设计测试(5条用例, 3天)
### 任务8: 浏览器兼容性测试(4条用例, 2天)
### 任务9: 性能基准测试(3条用例, 2天)

*(具体用例内容详见评审意见文档第4.7、4.8章节)*

---

## 执行时间表

```
第1周 (1月20-26日)
├─ 1月20日 (周一): 字段映射交互用例 (4条)
├─ 1月21日 (周二): 文件上传交互用例 (4条)
├─ 1月22日 (周三): 加载状态用例 (3条)
├─ 1月23日 (周四): 错误和空状态用例 (4条)
├─ 1月24日 (周五): 表格交互用例 (3条)
└─ 1月25-26日 (周末): 修改现有用例 (5条)

第2周 (1月27-2月2日)
├─ 1月27日 (周一): 审核和整合
├─ 1月28日 (周二): 补充响应式设计测试 (5条)
├─ 1月29日 (周三): 补充浏览器兼容性测试 (4条)
├─ 1月30日 (周四): 补充性能基准测试 (3条)
└─ 1月31-2月2日: 最终审核和发布
```

---

## 验收标准

### 每条用例的验收检查清单

- [ ] 用例编号唯一
- [ ] 用例标题清晰，不超过30字
- [ ] 优先级标记正确(P0/P1/P2)
- [ ] 前置条件完整且可达成
- [ ] 测试步骤详细，可重复执行
- [ ] 预期结果具体，易于判断通过/失败
- [ ] 包含交互验证步骤
- [ ] 包含视觉反馈验证
- [ ] 性能指标(如适用)明确
- [ ] 无与其他用例重复

### 文档更新要求

- [ ] 更新目录结构
- [ ] 更新用例统计表
- [ ] 更新覆盖率评分
- [ ] 更新测试执行优先级
- [ ] 新增用例索引

---

## 资源需求

| 资源 | 数量 | 说明 |
|------|------|------|
| 测试人员 | 2 | 一人编写用例，一人审核 |
| 前端开发 | 1 | 审核交互，协助模拟场景 |
| 浏览器/工具 | - | Chrome, Edge, Firefox, Safari |
| 测试数据 | 1套 | 10万行CSV、大文件模拟 |
| 网络模拟工具 | - | Chrome DevTools, Fiddler等 |

---

## 预期成果

**第1-2周完成后**:
- 新增用例：42条
- 修改用例：5条
- 总用例数：101条(原54+42+5)
- 覆盖率提升：58% -> 82%
- 交互测试覆盖：35% -> 75%
- 状态管理测试覆盖：20% -> 65%

**最终目标**:
- 总覆盖率：85%+
- 交互测试覆盖：85%+
- 状态管理覆盖：80%+
- 响应式覆盖：70%+
- 浏览器兼容性覆盖：60%+

---

## 风险管理

### 风险1: 用例编写进度落后

**应对措施**:
- 每日进行进度检查(10分钟)
- 优先完成P1用例
- 必要时加班或延期发布

### 风险2: 前端组件不支持某些交互

**应对措施**:
- 提前联系前端确认实现情况
- 制定备选方案(使用现有功能)
- 记录为"待实现"用例

### 风险3: 环境和工具不足

**应对措施**:
- 优先配置云端浏览器测试
- 使用开源工具替代商用工具
- 记录工具清单便于后续团队使用

---

## 执行负责人

- **用例编写**: 测试专家(TEST_AGENT)
- **用例审核**: 前端研发专家(FRONTEND_AGENT)
- **最终发布**: 项目经理(PM)

---

**方案生效日期**: 2026年1月20日
**预期完成日期**: 2026年2月2日

