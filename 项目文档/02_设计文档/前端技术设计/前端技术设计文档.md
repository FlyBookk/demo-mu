# 亚马逊转口贸易报税系统 - 前端技术设计文档

**版本**: v1.0
**创建日期**: 2026年1月19日
**作者**: 前端研发专家(FRONTEND_AGENT)
**项目名称**: 慕声亚马逊转口贸易报税管理系统

---

## 1. 技术选型

### 1.1 核心技术栈

| 类别 | 技术选型 | 版本 | 选型理由 |
|------|---------|------|---------|
| 框架 | Vue.js | 3.4+ | Composition API、性能优异、生态完善 |
| 语言 | TypeScript | 5.3+ | 类型安全、IDE支持好、维护性强 |
| 构建工具 | Vite | 5.x | 冷启动快、HMR即时、构建性能优 |
| UI组件库 | Ant Design Vue | 4.x | 企业级组件丰富、文档完善 |
| 状态管理 | Pinia | 2.x | Vue3官方推荐、TypeScript友好 |
| 路由 | Vue Router | 4.x | Vue3配套、功能完整 |
| HTTP客户端 | Axios | 1.6+ | 拦截器机制、广泛使用 |

### 1.2 辅助技术栈

| 类别 | 技术选型 | 用途 |
|------|---------|------|
| CSS预处理 | SCSS | 样式编写、变量管理 |
| 图表库 | ECharts 5.x | 数据可视化、Dashboard图表 |
| 拖拽库 | VueDraggable Plus | 字段映射画布拖拽交互 |
| 虚拟表格 | vxe-table 4.x | 10万行大数据表格渲染 |
| CSV解析 | PapaParse | 前端CSV文件解析 |
| 日期处理 | Day.js | 日期格式化、多时区处理 |
| 工具库 | Lodash-es | 防抖节流、数据处理 |
| 代码规范 | ESLint + Prettier | 代码质量保障 |
| 单元测试 | Vitest | 组件单元测试 |

### 1.3 浏览器兼容性

| 浏览器 | 最低版本 | 说明 |
|--------|---------|------|
| Chrome | 90+ | 主要支持 |
| Edge | 90+ | 主要支持 |
| Firefox | 90+ | 次要支持 |
| Safari | 14+ | 次要支持 |

---

## 2. 项目结构

### 2.1 目录结构

```
musheng-tax-web/
├── public/                          # 静态资源
│   ├── favicon.ico
│   └── config.js                    # 运行时配置
├── src/
│   ├── api/                         # API接口定义
│   │   ├── index.ts                 # API统一导出
│   │   ├── auth.ts                  # 认证相关
│   │   ├── currency.ts              # 货币管理
│   │   ├── marketplace.ts           # 站点管理
│   │   ├── transactionType.ts       # 交易类型映射
│   │   ├── fieldMapping.ts          # 字段映射模板
│   │   ├── sales.ts                 # 销售数据
│   │   ├── shipping.ts              # 配送数据
│   │   ├── advertising.ts           # 广告数据
│   │   ├── exchangeRate.ts          # 汇率管理
│   │   ├── importRecord.ts          # 导入记录
│   │   ├── report.ts                # 汇总报表
│   │   └── system.ts                # 系统管理
│   ├── assets/                      # 静态资源
│   │   ├── images/
│   │   └── styles/
│   │       ├── variables.scss       # SCSS变量
│   │       ├── mixins.scss          # SCSS混入
│   │       ├── reset.scss           # 样式重置
│   │       └── global.scss          # 全局样式
│   ├── components/                  # 公共组件
│   │   ├── common/                  # 通用基础组件
│   │   │   ├── AppHeader/
│   │   │   ├── AppSidebar/
│   │   │   ├── PageHeader/
│   │   │   ├── EmptyState/
│   │   │   ├── LoadingState/
│   │   │   └── ErrorState/
│   │   ├── business/                # 业务组件
│   │   │   ├── FieldMappingCanvas/  # 字段映射画布
│   │   │   ├── FileUpload/          # 文件上传组件
│   │   │   ├── DataTable/           # 数据表格组件
│   │   │   ├── SiteSelect/          # 站点选择器
│   │   │   ├── CurrencySelect/      # 货币选择器
│   │   │   ├── QuarterPicker/       # 季度选择器
│   │   │   ├── MoneyDisplay/        # 金额展示组件
│   │   │   └── ImportProgress/      # 导入进度组件
│   │   └── index.ts                 # 组件统一导出
│   ├── composables/                 # 组合式函数
│   │   ├── useAuth.ts               # 认证相关
│   │   ├── useTable.ts              # 表格通用逻辑
│   │   ├── useUpload.ts             # 上传相关
│   │   ├── useExport.ts             # 导出相关
│   │   ├── usePagination.ts         # 分页相关
│   │   ├── usePermission.ts         # 权限相关
│   │   └── useWebSocket.ts          # WebSocket连接
│   ├── constants/                   # 常量定义
│   │   ├── index.ts
│   │   ├── site.ts                  # 站点常量
│   │   ├── currency.ts              # 货币常量
│   │   ├── transactionType.ts       # 交易类型常量
│   │   └── permission.ts            # 权限常量
│   ├── directives/                  # 自定义指令
│   │   ├── permission.ts            # 权限指令
│   │   └── loading.ts               # 加载指令
│   ├── layouts/                     # 布局组件
│   │   ├── MainLayout.vue           # 主布局
│   │   ├── BlankLayout.vue          # 空白布局
│   │   └── components/
│   │       ├── LayoutHeader.vue
│   │       ├── LayoutSidebar.vue
│   │       └── LayoutBreadcrumb.vue
│   ├── router/                      # 路由配置
│   │   ├── index.ts                 # 路由入口
│   │   ├── routes.ts                # 路由定义
│   │   └── guards.ts                # 路由守卫
│   ├── stores/                      # 状态管理
│   │   ├── index.ts                 # Store统一导出
│   │   ├── modules/
│   │   │   ├── auth.ts              # 认证状态
│   │   │   ├── app.ts               # 应用状态
│   │   │   ├── currency.ts          # 货币数据
│   │   │   ├── marketplace.ts       # 站点数据
│   │   │   └── user.ts              # 用户信息
│   │   └── types.ts                 # Store类型
│   ├── types/                       # 类型定义
│   │   ├── index.ts
│   │   ├── api.ts                   # API响应类型
│   │   ├── auth.ts                  # 认证类型
│   │   ├── currency.ts              # 货币类型
│   │   ├── marketplace.ts           # 站点类型
│   │   ├── sales.ts                 # 销售数据类型
│   │   ├── shipping.ts              # 配送数据类型
│   │   ├── advertising.ts           # 广告数据类型
│   │   ├── exchangeRate.ts          # 汇率类型
│   │   ├── fieldMapping.ts          # 字段映射类型
│   │   ├── report.ts                # 报表类型
│   │   └── common.ts                # 通用类型
│   ├── utils/                       # 工具函数
│   │   ├── request.ts               # Axios封装
│   │   ├── auth.ts                  # 认证工具
│   │   ├── storage.ts               # 存储工具
│   │   ├── format.ts                # 格式化工具
│   │   ├── validate.ts              # 校验工具
│   │   ├── csv.ts                   # CSV处理工具
│   │   └── file.ts                  # 文件处理工具
│   ├── views/                       # 页面组件
│   │   ├── login/                   # 登录页
│   │   │   └── index.vue
│   │   ├── dashboard/               # 首页
│   │   │   └── index.vue
│   │   ├── config/                  # 基础配置
│   │   │   ├── currency/            # 货币管理
│   │   │   ├── marketplace/         # 站点管理
│   │   │   ├── transactionType/     # 交易类型映射
│   │   │   ├── fieldMapping/        # 字段映射模板
│   │   │   └── importRecord/        # 导入记录
│   │   ├── sales/                   # 销售数据
│   │   │   ├── import/              # 数据导入
│   │   │   └── list/                # 数据列表
│   │   ├── shipping/                # 配送数据
│   │   │   ├── import/
│   │   │   └── list/
│   │   ├── advertising/             # 广告数据
│   │   │   ├── add/
│   │   │   └── list/
│   │   ├── rate/                    # 汇率管理
│   │   │   ├── import/
│   │   │   └── list/
│   │   ├── report/                  # 汇总报表
│   │   │   ├── summary/
│   │   │   └── download/
│   │   └── system/                  # 系统管理
│   │       ├── user/
│   │       ├── role/
│   │       └── log/
│   ├── App.vue                      # 根组件
│   └── main.ts                      # 入口文件
├── .env.development                 # 开发环境变量
├── .env.production                  # 生产环境变量
├── .eslintrc.cjs                    # ESLint配置
├── .prettierrc                      # Prettier配置
├── index.html                       # HTML模板
├── package.json
├── tsconfig.json                    # TS配置
├── vite.config.ts                   # Vite配置
└── README.md
```

### 2.2 模块划分

| 模块 | 路径 | 说明 |
|------|------|------|
| 基础配置 | `/config/*` | 货币、站点、交易类型映射、字段映射、导入记录 |
| 销售数据 | `/sales/*` | 数据导入（含映射画布）、数据列表 |
| 配送数据 | `/shipping/*` | 数据导入、数据列表 |
| 广告数据 | `/advertising/*` | 广告费录入、数据列表 |
| 汇率管理 | `/rate/*` | 汇率导入、汇率查询 |
| 汇总报表 | `/report/*` | 汇总查询、报表下载 |
| 系统管理 | `/system/*` | 用户管理、权限管理、操作日志 |

---

## 3. 组件设计

### 3.1 公共组件清单

#### 3.1.1 通用基础组件

| 组件名 | 文件路径 | 功能描述 |
|--------|---------|---------|
| PageHeader | `components/common/PageHeader/` | 页面头部，含标题、面包屑、操作按钮 |
| EmptyState | `components/common/EmptyState/` | 空状态展示，含图标、文字、操作建议 |
| LoadingState | `components/common/LoadingState/` | 加载状态展示，骨架屏/Spin |
| ErrorState | `components/common/ErrorState/` | 错误状态展示，含重试按钮 |
| ConfirmModal | `components/common/ConfirmModal/` | 确认弹窗，危险操作二次确认 |

#### 3.1.2 业务组件

| 组件名 | 文件路径 | 功能描述 |
|--------|---------|---------|
| FieldMappingCanvas | `components/business/FieldMappingCanvas/` | 字段映射画布，核心组件 |
| FileUpload | `components/business/FileUpload/` | 文件上传，支持分片、进度 |
| DataTable | `components/business/DataTable/` | 数据表格，虚拟滚动 |
| SiteSelect | `components/business/SiteSelect/` | 站点选择器 |
| CurrencySelect | `components/business/CurrencySelect/` | 货币选择器 |
| QuarterPicker | `components/business/QuarterPicker/` | 季度选择器 |
| MoneyDisplay | `components/business/MoneyDisplay/` | 金额展示，含货币符号 |
| ImportProgress | `components/business/ImportProgress/` | 导入进度展示 |

### 3.2 字段映射画布组件详细设计

#### 3.2.1 组件概述

字段映射画布是本系统的核心交互组件，用于可视化配置CSV文件字段与系统数据表字段的映射关系。

**功能特性**:
- 左侧展示CSV文件解析出的字段列表（含示例数据）
- 右侧展示系统目标字段列表（含字段说明、必填标识、默认值）
- 中间区域SVG绘制映射连线
- 支持拖拽建立映射关系
- 支持点击选择建立映射关系
- 支持删除已建立的映射
- 支持保存为站点映射模板
- 支持加载已有映射模板

#### 3.2.2 组件结构

```
components/business/FieldMappingCanvas/
├── index.vue                    # 主组件
├── SourceFieldList.vue          # 左侧源字段列表
├── TargetFieldList.vue          # 右侧目标字段列表
├── MappingLines.vue             # 中间连线区域
├── FieldItem.vue                # 字段项组件
├── MappingToolbar.vue           # 工具栏（保存、加载、重置）
├── types.ts                     # 类型定义
├── hooks/
│   ├── useDragMapping.ts        # 拖拽映射逻辑
│   ├── useClickMapping.ts       # 点击映射逻辑
│   ├── useLineCalculation.ts    # 连线计算逻辑
│   └── useMappingTemplate.ts    # 模板管理逻辑
└── style.scss                   # 样式文件
```

#### 3.2.3 类型定义

```typescript
// components/business/FieldMappingCanvas/types.ts

/** CSV源字段 */
export interface SourceField {
  /** 字段名称（CSV表头） */
  name: string
  /** 示例数据（首行数据值） */
  sample: string
  /** 字段索引 */
  index: number
  /** 是否已映射 */
  mapped?: boolean
}

/** 系统目标字段 */
export interface TargetField {
  /** 字段名（数据库字段名） */
  name: string
  /** 显示标签 */
  label: string
  /** 字段描述 */
  description: string
  /** 是否必填 */
  required: boolean
  /** 默认值 */
  defaultValue?: string | number
  /** 数据类型 */
  dataType: 'string' | 'number' | 'datetime'
  /** 是否已映射 */
  mapped?: boolean
  /** 映射的源字段名 */
  mappedSource?: string
}

/** 映射配置项 */
export interface MappingConfig {
  /** 源字段名 */
  source: string
  /** 目标字段名 */
  target: string
}

/** 映射模板 */
export interface MappingTemplate {
  /** 模板ID */
  id: number
  /** 模板名称 */
  templateName: string
  /** 站点编码 */
  siteCode: string
  /** 映射配置 */
  mappings: MappingConfig[]
  /** 未映射字段默认值 */
  unmappedDefaults: Record<string, string | number>
  /** 是否默认模板 */
  isDefault: boolean
}

/** 连线数据 */
export interface MappingLine {
  /** 源字段名 */
  source: string
  /** 目标字段名 */
  target: string
  /** SVG路径 */
  path: string
  /** 源点坐标 */
  sourcePoint: { x: number; y: number }
  /** 目标点坐标 */
  targetPoint: { x: number; y: number }
}

/** 组件Props */
export interface FieldMappingCanvasProps {
  /** CSV源字段列表 */
  sourceFields: SourceField[]
  /** 系统目标字段列表 */
  targetFields: TargetField[]
  /** 初始映射配置 */
  initialMappings?: MappingConfig[]
  /** 站点编码 */
  siteCode?: string
  /** 是否只读 */
  readonly?: boolean
}

/** 组件Emits */
export interface FieldMappingCanvasEmits {
  (e: 'update:mappings', mappings: MappingConfig[]): void
  (e: 'save-template', template: Partial<MappingTemplate>): void
  (e: 'load-template', templateId: number): void
}
```

#### 3.2.4 主组件代码

```vue
<!-- components/business/FieldMappingCanvas/index.vue -->
<template>
  <div class="field-mapping-canvas">
    <!-- 工具栏 -->
    <MappingToolbar
      :site-code="siteCode"
      :has-changes="hasChanges"
      :mapping-count="mappings.length"
      :unmapped-required-count="unmappedRequiredCount"
      @save="handleSaveTemplate"
      @load="handleLoadTemplate"
      @reset="handleReset"
      @auto-map="handleAutoMap"
    />

    <!-- 映射区域 -->
    <div class="mapping-area" ref="mappingAreaRef">
      <!-- 左侧：CSV字段列表 -->
      <SourceFieldList
        :fields="sourceFieldsWithStatus"
        :selected-field="selectedSourceField"
        :readonly="readonly"
        @select="handleSourceSelect"
        @drag-start="handleDragStart"
        @drag-end="handleDragEnd"
      />

      <!-- 中间：连线区域 -->
      <MappingLines
        ref="mappingLinesRef"
        :lines="mappingLines"
        :active-line="activeLine"
        :dragging="isDragging"
        :drag-position="dragPosition"
        @line-click="handleLineClick"
        @line-delete="handleDeleteMapping"
      />

      <!-- 右侧：系统字段列表 -->
      <TargetFieldList
        :fields="targetFieldsWithStatus"
        :selected-field="selectedTargetField"
        :readonly="readonly"
        @select="handleTargetSelect"
        @drop="handleDrop"
      />
    </div>

    <!-- 未映射必填字段提示 -->
    <div v-if="unmappedRequiredCount > 0" class="unmapped-warning">
      <a-alert
        type="warning"
        :message="`还有 ${unmappedRequiredCount} 个必填字段未映射`"
        show-icon
      />
    </div>

    <!-- 映射统计 -->
    <div class="mapping-stats">
      <a-space>
        <a-tag color="green">已映射: {{ mappings.length }}</a-tag>
        <a-tag color="orange">未映射源字段: {{ unmappedSourceCount }}</a-tag>
        <a-tag :color="unmappedRequiredCount > 0 ? 'red' : 'blue'">
          未映射必填: {{ unmappedRequiredCount }}
        </a-tag>
      </a-space>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, watch, onMounted, onUnmounted } from 'vue'
import { message } from 'ant-design-vue'
import SourceFieldList from './SourceFieldList.vue'
import TargetFieldList from './TargetFieldList.vue'
import MappingLines from './MappingLines.vue'
import MappingToolbar from './MappingToolbar.vue'
import { useDragMapping } from './hooks/useDragMapping'
import { useClickMapping } from './hooks/useClickMapping'
import { useLineCalculation } from './hooks/useLineCalculation'
import type {
  FieldMappingCanvasProps,
  MappingConfig,
  SourceField,
  TargetField,
  MappingLine
} from './types'

const props = withDefaults(defineProps<FieldMappingCanvasProps>(), {
  initialMappings: () => [],
  readonly: false
})

const emit = defineEmits<{
  (e: 'update:mappings', mappings: MappingConfig[]): void
  (e: 'save-template', mappings: MappingConfig[]): void
  (e: 'load-template', templateId: number): void
}>()

// Refs
const mappingAreaRef = ref<HTMLElement>()
const mappingLinesRef = ref<InstanceType<typeof MappingLines>>()

// 映射数据
const mappings = ref<MappingConfig[]>([...props.initialMappings])
const originalMappings = ref<MappingConfig[]>([...props.initialMappings])

// 拖拽相关
const {
  isDragging,
  dragPosition,
  dragSourceField,
  handleDragStart,
  handleDragEnd,
  handleDrop: onDrop
} = useDragMapping()

// 点击选择相关
const {
  selectedSourceField,
  selectedTargetField,
  handleSourceSelect,
  handleTargetSelect,
  clearSelection
} = useClickMapping()

// 连线计算
const { calculateLines, activeLine } = useLineCalculation(mappingAreaRef)

// 计算属性：带映射状态的源字段
const sourceFieldsWithStatus = computed<SourceField[]>(() => {
  return props.sourceFields.map(field => ({
    ...field,
    mapped: mappings.value.some(m => m.source === field.name)
  }))
})

// 计算属性：带映射状态的目标字段
const targetFieldsWithStatus = computed<TargetField[]>(() => {
  return props.targetFields.map(field => {
    const mapping = mappings.value.find(m => m.target === field.name)
    return {
      ...field,
      mapped: !!mapping,
      mappedSource: mapping?.source
    }
  })
})

// 计算属性：映射连线
const mappingLines = computed<MappingLine[]>(() => {
  return calculateLines(
    mappings.value,
    sourceFieldsWithStatus.value,
    targetFieldsWithStatus.value
  )
})

// 计算属性：未映射必填字段数
const unmappedRequiredCount = computed(() => {
  return props.targetFields.filter(
    f => f.required && !mappings.value.some(m => m.target === f.name)
  ).length
})

// 计算属性：未映射源字段数
const unmappedSourceCount = computed(() => {
  return props.sourceFields.filter(
    f => !mappings.value.some(m => m.source === f.name)
  ).length
})

// 计算属性：是否有变更
const hasChanges = computed(() => {
  return JSON.stringify(mappings.value) !== JSON.stringify(originalMappings.value)
})

// 添加映射
const addMapping = (source: string, target: string) => {
  // 移除目标字段已有的映射
  const existingIndex = mappings.value.findIndex(m => m.target === target)
  if (existingIndex > -1) {
    mappings.value.splice(existingIndex, 1)
  }
  // 移除源字段已有的映射
  const sourceIndex = mappings.value.findIndex(m => m.source === source)
  if (sourceIndex > -1) {
    mappings.value.splice(sourceIndex, 1)
  }
  // 添加新映射
  mappings.value.push({ source, target })
  emit('update:mappings', mappings.value)
  clearSelection()
}

// 删除映射
const handleDeleteMapping = (mapping: MappingConfig) => {
  const index = mappings.value.findIndex(
    m => m.source === mapping.source && m.target === mapping.target
  )
  if (index > -1) {
    mappings.value.splice(index, 1)
    emit('update:mappings', mappings.value)
  }
}

// 处理拖拽放置
const handleDrop = (targetField: TargetField) => {
  if (dragSourceField.value) {
    addMapping(dragSourceField.value.name, targetField.name)
  }
  handleDragEnd()
}

// 处理点击选择
watch([selectedSourceField, selectedTargetField], ([source, target]) => {
  if (source && target) {
    addMapping(source.name, target.name)
  }
})

// 处理连线点击
const handleLineClick = (line: MappingLine) => {
  activeLine.value = line
}

// 保存模板
const handleSaveTemplate = () => {
  if (unmappedRequiredCount.value > 0) {
    message.warning('还有必填字段未映射，建议完成映射后再保存')
  }
  emit('save-template', mappings.value)
  originalMappings.value = [...mappings.value]
}

// 加载模板
const handleLoadTemplate = (templateId: number) => {
  emit('load-template', templateId)
}

// 重置映射
const handleReset = () => {
  mappings.value = [...originalMappings.value]
  emit('update:mappings', mappings.value)
  clearSelection()
}

// 自动映射（按字段名相似度匹配）
const handleAutoMap = () => {
  const autoMappings: MappingConfig[] = []

  props.sourceFields.forEach(source => {
    const sourceLower = source.name.toLowerCase().replace(/[\s_-]/g, '')

    // 查找最匹配的目标字段
    const matchedTarget = props.targetFields.find(target => {
      const targetLower = target.name.toLowerCase().replace(/[\s_-]/g, '')
      const labelLower = target.label.toLowerCase().replace(/[\s_-]/g, '')
      return sourceLower === targetLower || sourceLower === labelLower ||
             sourceLower.includes(targetLower) || targetLower.includes(sourceLower)
    })

    if (matchedTarget && !autoMappings.some(m => m.target === matchedTarget.name)) {
      autoMappings.push({ source: source.name, target: matchedTarget.name })
    }
  })

  if (autoMappings.length > 0) {
    mappings.value = autoMappings
    emit('update:mappings', mappings.value)
    message.success(`自动匹配了 ${autoMappings.length} 个字段`)
  } else {
    message.info('未找到可自动匹配的字段')
  }
}

// 监听初始映射变化
watch(() => props.initialMappings, (newMappings) => {
  mappings.value = [...newMappings]
  originalMappings.value = [...newMappings]
}, { deep: true })

// 窗口大小变化时重新计算连线
onMounted(() => {
  window.addEventListener('resize', () => mappingLinesRef.value?.recalculate())
})

onUnmounted(() => {
  window.removeEventListener('resize', () => mappingLinesRef.value?.recalculate())
})
</script>

<style lang="scss" scoped>
.field-mapping-canvas {
  display: flex;
  flex-direction: column;
  height: 100%;
  background: #fafafa;
  border-radius: 8px;
  padding: 16px;

  .mapping-area {
    display: flex;
    flex: 1;
    min-height: 400px;
    gap: 0;
    position: relative;
  }

  .unmapped-warning {
    margin-top: 16px;
  }

  .mapping-stats {
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid #e8e8e8;
  }
}
</style>
```

#### 3.2.5 连线计算Hook

```typescript
// components/business/FieldMappingCanvas/hooks/useLineCalculation.ts
import { ref, type Ref } from 'vue'
import type { MappingConfig, SourceField, TargetField, MappingLine } from '../types'

export function useLineCalculation(containerRef: Ref<HTMLElement | undefined>) {
  const activeLine = ref<MappingLine | null>(null)

  /**
   * 计算贝塞尔曲线路径
   */
  const calculateBezierPath = (
    x1: number, y1: number,
    x2: number, y2: number
  ): string => {
    const midX = (x1 + x2) / 2
    // 贝塞尔曲线控制点
    const cp1x = midX
    const cp1y = y1
    const cp2x = midX
    const cp2y = y2
    return `M ${x1} ${y1} C ${cp1x} ${cp1y}, ${cp2x} ${cp2y}, ${x2} ${y2}`
  }

  /**
   * 获取字段元素位置
   */
  const getFieldPosition = (
    fieldName: string,
    type: 'source' | 'target'
  ): { x: number; y: number } | null => {
    if (!containerRef.value) return null

    const selector = type === 'source'
      ? `[data-source-field="${fieldName}"]`
      : `[data-target-field="${fieldName}"]`

    const element = containerRef.value.querySelector(selector)
    if (!element) return null

    const containerRect = containerRef.value.getBoundingClientRect()
    const elementRect = element.getBoundingClientRect()

    const x = type === 'source'
      ? elementRect.right - containerRect.left
      : elementRect.left - containerRect.left
    const y = elementRect.top - containerRect.top + elementRect.height / 2

    return { x, y }
  }

  /**
   * 计算所有映射连线
   */
  const calculateLines = (
    mappings: MappingConfig[],
    sourceFields: SourceField[],
    targetFields: TargetField[]
  ): MappingLine[] => {
    const lines: MappingLine[] = []

    mappings.forEach(mapping => {
      const sourcePos = getFieldPosition(mapping.source, 'source')
      const targetPos = getFieldPosition(mapping.target, 'target')

      if (sourcePos && targetPos) {
        lines.push({
          source: mapping.source,
          target: mapping.target,
          sourcePoint: sourcePos,
          targetPoint: targetPos,
          path: calculateBezierPath(
            sourcePos.x, sourcePos.y,
            targetPos.x, targetPos.y
          )
        })
      }
    })

    return lines
  }

  return {
    activeLine,
    calculateLines
  }
}
```

### 3.3 文件上传组件设计

#### 3.3.1 组件结构

```
components/business/FileUpload/
├── index.vue                    # 主组件
├── ChunkUpload.vue              # 分片上传组件
├── UploadProgress.vue           # 上传进度组件
├── types.ts                     # 类型定义
├── hooks/
│   ├── useChunkUpload.ts        # 分片上传逻辑
│   └── useFileValidation.ts     # 文件校验逻辑
└── style.scss
```

#### 3.3.2 分片上传Hook

```typescript
// components/business/FileUpload/hooks/useChunkUpload.ts
import { ref, computed } from 'vue'
import SparkMD5 from 'spark-md5'

export interface ChunkUploadOptions {
  /** 分片大小（字节），默认2MB */
  chunkSize?: number
  /** 最大并发数 */
  maxConcurrent?: number
  /** 上传API */
  uploadApi: (chunk: Blob, params: ChunkParams) => Promise<any>
  /** 合并API */
  mergeApi: (params: MergeParams) => Promise<any>
}

export interface ChunkParams {
  file: File
  chunk: Blob
  chunkIndex: number
  totalChunks: number
  fileHash: string
  fileName: string
}

export interface MergeParams {
  fileHash: string
  fileName: string
  totalChunks: number
}

export function useChunkUpload(options: ChunkUploadOptions) {
  const {
    chunkSize = 2 * 1024 * 1024, // 2MB
    maxConcurrent = 3,
    uploadApi,
    mergeApi
  } = options

  const uploading = ref(false)
  const progress = ref(0)
  const uploadedChunks = ref<number[]>([])
  const currentFile = ref<File | null>(null)
  const fileHash = ref('')
  const error = ref<Error | null>(null)

  /**
   * 计算文件MD5
   */
  const calculateFileHash = (file: File): Promise<string> => {
    return new Promise((resolve, reject) => {
      const spark = new SparkMD5.ArrayBuffer()
      const reader = new FileReader()
      const chunks = Math.ceil(file.size / chunkSize)
      let currentChunk = 0

      reader.onload = (e) => {
        spark.append(e.target?.result as ArrayBuffer)
        currentChunk++

        if (currentChunk < chunks) {
          loadNext()
        } else {
          resolve(spark.end())
        }
      }

      reader.onerror = () => reject(new Error('文件读取失败'))

      const loadNext = () => {
        const start = currentChunk * chunkSize
        const end = Math.min(start + chunkSize, file.size)
        reader.readAsArrayBuffer(file.slice(start, end))
      }

      loadNext()
    })
  }

  /**
   * 创建分片
   */
  const createChunks = (file: File): Blob[] => {
    const chunks: Blob[] = []
    let start = 0

    while (start < file.size) {
      const end = Math.min(start + chunkSize, file.size)
      chunks.push(file.slice(start, end))
      start = end
    }

    return chunks
  }

  /**
   * 上传单个分片
   */
  const uploadChunk = async (
    file: File,
    chunk: Blob,
    index: number,
    totalChunks: number,
    hash: string
  ): Promise<void> => {
    await uploadApi(chunk, {
      file,
      chunk,
      chunkIndex: index,
      totalChunks,
      fileHash: hash,
      fileName: file.name
    })
    uploadedChunks.value.push(index)
    progress.value = Math.floor((uploadedChunks.value.length / totalChunks) * 100)
  }

  /**
   * 并发控制上传
   */
  const uploadWithConcurrency = async (
    file: File,
    chunks: Blob[],
    hash: string
  ): Promise<void> => {
    const totalChunks = chunks.length
    let currentIndex = 0

    const uploadNext = async (): Promise<void> => {
      while (currentIndex < totalChunks) {
        const index = currentIndex++
        if (!uploadedChunks.value.includes(index)) {
          await uploadChunk(file, chunks[index], index, totalChunks, hash)
        }
      }
    }

    // 启动并发上传
    const tasks = Array(Math.min(maxConcurrent, totalChunks))
      .fill(null)
      .map(() => uploadNext())

    await Promise.all(tasks)
  }

  /**
   * 开始上传
   */
  const startUpload = async (file: File): Promise<void> => {
    try {
      uploading.value = true
      error.value = null
      progress.value = 0
      uploadedChunks.value = []
      currentFile.value = file

      // 计算文件哈希
      fileHash.value = await calculateFileHash(file)

      // 创建分片
      const chunks = createChunks(file)

      // 上传分片
      await uploadWithConcurrency(file, chunks, fileHash.value)

      // 合并文件
      await mergeApi({
        fileHash: fileHash.value,
        fileName: file.name,
        totalChunks: chunks.length
      })

      progress.value = 100
    } catch (err) {
      error.value = err as Error
      throw err
    } finally {
      uploading.value = false
    }
  }

  /**
   * 暂停上传
   */
  const pauseUpload = () => {
    uploading.value = false
  }

  /**
   * 恢复上传
   */
  const resumeUpload = async (): Promise<void> => {
    if (!currentFile.value) return

    uploading.value = true
    const chunks = createChunks(currentFile.value)
    await uploadWithConcurrency(currentFile.value, chunks, fileHash.value)
    await mergeApi({
      fileHash: fileHash.value,
      fileName: currentFile.value.name,
      totalChunks: chunks.length
    })
    progress.value = 100
    uploading.value = false
  }

  return {
    uploading,
    progress,
    uploadedChunks,
    error,
    startUpload,
    pauseUpload,
    resumeUpload
  }
}
```

#### 3.3.3 文件上传主组件

```vue
<!-- components/business/FileUpload/index.vue -->
<template>
  <div class="file-upload">
    <a-upload-dragger
      v-if="!file"
      :accept="accept"
      :before-upload="handleBeforeUpload"
      :show-upload-list="false"
    >
      <p class="ant-upload-drag-icon">
        <inbox-outlined />
      </p>
      <p class="ant-upload-text">点击或拖拽文件到此区域上传</p>
      <p class="ant-upload-hint">{{ hint }}</p>
    </a-upload-dragger>

    <!-- 文件信息和进度 -->
    <div v-else class="upload-info">
      <div class="file-info">
        <file-excel-outlined class="file-icon" />
        <div class="file-detail">
          <span class="file-name">{{ file.name }}</span>
          <span class="file-size">{{ formatFileSize(file.size) }}</span>
        </div>
        <a-button
          v-if="!uploading"
          type="text"
          danger
          @click="handleRemove"
        >
          <delete-outlined />
        </a-button>
      </div>

      <!-- 上传进度 -->
      <div v-if="uploading || progress > 0" class="upload-progress">
        <a-progress
          :percent="progress"
          :status="progressStatus"
          :stroke-color="progressColor"
        />
        <div class="progress-text">
          <span v-if="uploading">正在上传...</span>
          <span v-else-if="progress === 100">上传完成</span>
          <span v-else-if="error">上传失败: {{ error.message }}</span>
        </div>
      </div>

      <!-- 操作按钮 -->
      <div class="upload-actions">
        <a-space>
          <a-button
            v-if="!uploading && progress === 0"
            type="primary"
            @click="handleStartUpload"
          >
            开始上传
          </a-button>
          <a-button
            v-if="uploading"
            @click="handlePause"
          >
            暂停
          </a-button>
          <a-button
            v-if="!uploading && progress > 0 && progress < 100"
            type="primary"
            @click="handleResume"
          >
            继续上传
          </a-button>
          <a-button
            v-if="error"
            type="primary"
            @click="handleRetry"
          >
            重试
          </a-button>
        </a-space>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed } from 'vue'
import {
  InboxOutlined,
  FileExcelOutlined,
  DeleteOutlined
} from '@ant-design/icons-vue'
import { message } from 'ant-design-vue'
import { useChunkUpload } from './hooks/useChunkUpload'
import { uploadChunk, mergeChunks } from '@/api/upload'

const props = withDefaults(defineProps<{
  accept?: string
  maxSize?: number // MB
  hint?: string
  useChunk?: boolean
  chunkSize?: number // MB
}>(), {
  accept: '.csv,.xlsx,.xls',
  maxSize: 100,
  hint: '支持CSV、Excel格式文件，最大100MB',
  useChunk: true,
  chunkSize: 2
})

const emit = defineEmits<{
  (e: 'success', response: any): void
  (e: 'error', error: Error): void
  (e: 'file-selected', file: File): void
  (e: 'headers-parsed', headers: string[]): void
}>()

const file = ref<File | null>(null)

// 分片上传
const {
  uploading,
  progress,
  error,
  startUpload,
  pauseUpload,
  resumeUpload
} = useChunkUpload({
  chunkSize: props.chunkSize * 1024 * 1024,
  uploadApi: uploadChunk,
  mergeApi: mergeChunks
})

// 进度状态
const progressStatus = computed(() => {
  if (error.value) return 'exception'
  if (progress.value === 100) return 'success'
  return 'active'
})

// 进度颜色
const progressColor = computed(() => {
  if (error.value) return '#ff4d4f'
  if (progress.value === 100) return '#52c41a'
  return '#1890ff'
})

// 格式化文件大小
const formatFileSize = (bytes: number): string => {
  if (bytes < 1024) return bytes + ' B'
  if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(2) + ' KB'
  return (bytes / (1024 * 1024)).toFixed(2) + ' MB'
}

// 上传前校验
const handleBeforeUpload = (uploadFile: File): boolean => {
  // 校验文件类型
  const validTypes = props.accept.split(',').map(t => t.trim())
  const fileExt = '.' + uploadFile.name.split('.').pop()?.toLowerCase()
  if (!validTypes.includes(fileExt)) {
    message.error(`只支持 ${props.accept} 格式的文件`)
    return false
  }

  // 校验文件大小
  if (uploadFile.size > props.maxSize * 1024 * 1024) {
    message.error(`文件大小不能超过 ${props.maxSize}MB`)
    return false
  }

  file.value = uploadFile
  emit('file-selected', uploadFile)
  return false // 阻止默认上传
}

// 开始上传
const handleStartUpload = async () => {
  if (!file.value) return
  try {
    await startUpload(file.value)
    emit('success', { fileName: file.value.name })
    message.success('文件上传成功')
  } catch (err) {
    emit('error', err as Error)
    message.error('文件上传失败')
  }
}

// 暂停上传
const handlePause = () => {
  pauseUpload()
}

// 继续上传
const handleResume = async () => {
  await resumeUpload()
}

// 重试
const handleRetry = async () => {
  await handleStartUpload()
}

// 移除文件
const handleRemove = () => {
  file.value = null
  progress.value = 0
  error.value = null
}
</script>

<style lang="scss" scoped>
.file-upload {
  .upload-info {
    padding: 16px;
    border: 1px dashed #d9d9d9;
    border-radius: 8px;
    background: #fafafa;

    .file-info {
      display: flex;
      align-items: center;
      gap: 12px;

      .file-icon {
        font-size: 32px;
        color: #52c41a;
      }

      .file-detail {
        flex: 1;
        display: flex;
        flex-direction: column;

        .file-name {
          font-weight: 500;
        }

        .file-size {
          font-size: 12px;
          color: #999;
        }
      }
    }

    .upload-progress {
      margin-top: 16px;

      .progress-text {
        margin-top: 8px;
        font-size: 12px;
        color: #666;
      }
    }

    .upload-actions {
      margin-top: 16px;
    }
  }
}
</style>
```

### 3.4 数据表格组件设计（虚拟滚动方案）

#### 3.4.1 组件结构

```
components/business/DataTable/
├── index.vue                    # 主组件
├── VirtualTable.vue             # 虚拟滚动表格
├── FilterBar.vue                # 筛选栏
├── ActionBar.vue                # 操作栏
├── ColumnSetting.vue            # 列设置
├── types.ts                     # 类型定义
├── hooks/
│   ├── useVirtualScroll.ts      # 虚拟滚动逻辑
│   ├── useTableFilter.ts        # 筛选逻辑
│   └── useTableExport.ts        # 导出逻辑
└── style.scss
```

#### 3.4.2 虚拟滚动表格组件

```vue
<!-- components/business/DataTable/VirtualTable.vue -->
<template>
  <div class="virtual-table-wrapper">
    <vxe-table
      ref="tableRef"
      :data="tableData"
      :columns="tableColumns"
      :height="height"
      :loading="loading"
      :row-config="{ isHover: true, keyField: 'id' }"
      :scroll-y="{ enabled: true, gt: 100 }"
      :scroll-x="{ enabled: true }"
      :checkbox-config="{ checkField: 'checked', highlight: true }"
      :sort-config="{ trigger: 'cell', remote: true }"
      :filter-config="{ remote: true }"
      border
      show-overflow
      @checkbox-change="handleCheckboxChange"
      @checkbox-all="handleCheckboxAll"
      @sort-change="handleSortChange"
    >
      <!-- 序号列 -->
      <vxe-column type="seq" width="60" fixed="left" />

      <!-- 复选框列 -->
      <vxe-column
        v-if="showCheckbox"
        type="checkbox"
        width="50"
        fixed="left"
      />

      <!-- 动态列 -->
      <vxe-column
        v-for="col in visibleColumns"
        :key="col.field"
        :field="col.field"
        :title="col.title"
        :width="col.width"
        :min-width="col.minWidth || 100"
        :fixed="col.fixed"
        :sortable="col.sortable"
        :filters="col.filters"
        :formatter="col.formatter"
        :align="col.align || 'left'"
      >
        <template v-if="col.slot" #default="{ row }">
          <slot :name="col.slot" :row="row" :column="col" />
        </template>
      </vxe-column>

      <!-- 操作列 -->
      <vxe-column
        v-if="showAction"
        title="操作"
        :width="actionWidth"
        fixed="right"
        align="center"
      >
        <template #default="{ row }">
          <slot name="action" :row="row" />
        </template>
      </vxe-column>
    </vxe-table>

    <!-- 分页 -->
    <div v-if="showPagination" class="table-pagination">
      <a-pagination
        v-model:current="currentPage"
        v-model:pageSize="pageSize"
        :total="total"
        :show-size-changer="true"
        :show-quick-jumper="true"
        :show-total="(total) => `共 ${total} 条`"
        @change="handlePageChange"
        @showSizeChange="handleSizeChange"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, watch } from 'vue'
import { VxeTableInstance } from 'vxe-table'

export interface TableColumn {
  field: string
  title: string
  width?: number | string
  minWidth?: number
  fixed?: 'left' | 'right'
  sortable?: boolean
  filters?: any[]
  formatter?: (params: any) => string
  align?: 'left' | 'center' | 'right'
  slot?: string
  visible?: boolean
}

const props = withDefaults(defineProps<{
  data: any[]
  columns: TableColumn[]
  loading?: boolean
  height?: number | string
  total?: number
  showCheckbox?: boolean
  showAction?: boolean
  actionWidth?: number
  showPagination?: boolean
}>(), {
  loading: false,
  height: 500,
  total: 0,
  showCheckbox: true,
  showAction: true,
  actionWidth: 150,
  showPagination: true
})

const emit = defineEmits<{
  (e: 'selection-change', rows: any[]): void
  (e: 'sort-change', params: { field: string; order: string }): void
  (e: 'page-change', params: { page: number; pageSize: number }): void
}>()

const tableRef = ref<VxeTableInstance>()
const currentPage = ref(1)
const pageSize = ref(20)
const selectedRows = ref<any[]>([])

// 可见列
const visibleColumns = computed(() => {
  return props.columns.filter(col => col.visible !== false)
})

// 表格数据
const tableData = computed(() => props.data)

// 表格列配置
const tableColumns = computed(() => visibleColumns.value)

// 复选框变化
const handleCheckboxChange = ({ records }: { records: any[] }) => {
  selectedRows.value = records
  emit('selection-change', records)
}

// 全选变化
const handleCheckboxAll = ({ records }: { records: any[] }) => {
  selectedRows.value = records
  emit('selection-change', records)
}

// 排序变化
const handleSortChange = ({ field, order }: { field: string; order: string }) => {
  emit('sort-change', { field, order })
}

// 分页变化
const handlePageChange = (page: number, size: number) => {
  emit('page-change', { page, pageSize: size })
}

// 每页条数变化
const handleSizeChange = (current: number, size: number) => {
  currentPage.value = 1
  emit('page-change', { page: 1, pageSize: size })
}

// 暴露方法
defineExpose({
  getSelectedRows: () => selectedRows.value,
  clearSelection: () => {
    tableRef.value?.clearCheckboxRow()
    selectedRows.value = []
  },
  refreshTable: () => {
    tableRef.value?.refreshColumn()
  }
})
</script>

<style lang="scss" scoped>
.virtual-table-wrapper {
  .table-pagination {
    margin-top: 16px;
    display: flex;
    justify-content: flex-end;
  }
}
</style>
```

---

## 4. 状态管理

### 4.1 Store设计概览

| Store | 文件 | 职责 |
|-------|------|------|
| authStore | `stores/modules/auth.ts` | 认证状态、Token管理 |
| appStore | `stores/modules/app.ts` | 应用全局状态、菜单折叠 |
| userStore | `stores/modules/user.ts` | 用户信息、权限 |
| currencyStore | `stores/modules/currency.ts` | 货币列表缓存 |
| marketplaceStore | `stores/modules/marketplace.ts` | 站点列表缓存 |

### 4.2 认证Store

```typescript
// stores/modules/auth.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { login, logout, refreshToken } from '@/api/auth'
import { getToken, setToken, removeToken } from '@/utils/auth'
import type { LoginParams, UserInfo } from '@/types/auth'

export const useAuthStore = defineStore('auth', () => {
  // State
  const token = ref<string>(getToken() || '')
  const userInfo = ref<UserInfo | null>(null)
  const permissions = ref<string[]>([])

  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const isAdmin = computed(() => userInfo.value?.role === 'admin')
  const isFinance = computed(() => userInfo.value?.role === 'finance')

  // Actions
  const loginAction = async (params: LoginParams) => {
    try {
      const res = await login(params)
      token.value = res.token
      userInfo.value = res.userInfo
      permissions.value = res.permissions
      setToken(res.token)
      return res
    } catch (error) {
      throw error
    }
  }

  const logoutAction = async () => {
    try {
      await logout()
    } finally {
      token.value = ''
      userInfo.value = null
      permissions.value = []
      removeToken()
    }
  }

  const refreshTokenAction = async () => {
    try {
      const res = await refreshToken()
      token.value = res.token
      setToken(res.token)
      return res
    } catch (error) {
      await logoutAction()
      throw error
    }
  }

  const hasPermission = (permission: string): boolean => {
    if (isAdmin.value) return true
    return permissions.value.includes(permission)
  }

  return {
    token,
    userInfo,
    permissions,
    isLoggedIn,
    isAdmin,
    isFinance,
    loginAction,
    logoutAction,
    refreshTokenAction,
    hasPermission
  }
})
```

### 4.3 站点Store

```typescript
// stores/modules/marketplace.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { getMarketplaceList } from '@/api/marketplace'
import type { Marketplace } from '@/types/marketplace'

export const useMarketplaceStore = defineStore('marketplace', () => {
  // State
  const marketplaceList = ref<Marketplace[]>([])
  const loading = ref(false)
  const loaded = ref(false)

  // Getters
  const enabledMarketplaces = computed(() => {
    return marketplaceList.value.filter(m => m.status === 1)
  })

  const marketplaceMap = computed(() => {
    const map = new Map<string, Marketplace>()
    marketplaceList.value.forEach(m => {
      map.set(m.siteCode, m)
    })
    return map
  })

  // Actions
  const fetchMarketplaces = async (force = false) => {
    if (loaded.value && !force) return marketplaceList.value

    loading.value = true
    try {
      const res = await getMarketplaceList()
      marketplaceList.value = res.data
      loaded.value = true
      return res.data
    } finally {
      loading.value = false
    }
  }

  const getMarketplaceBySiteCode = (siteCode: string): Marketplace | undefined => {
    return marketplaceMap.value.get(siteCode)
  }

  const getMarketplaceByMarketplaceId = (marketplaceId: string): Marketplace | undefined => {
    return marketplaceList.value.find(
      m => m.marketplaceId.toLowerCase() === marketplaceId.toLowerCase()
    )
  }

  return {
    marketplaceList,
    loading,
    loaded,
    enabledMarketplaces,
    marketplaceMap,
    fetchMarketplaces,
    getMarketplaceBySiteCode,
    getMarketplaceByMarketplaceId
  }
})
```

---

## 5. 路由设计

### 5.1 路由配置

```typescript
// router/routes.ts
import type { RouteRecordRaw } from 'vue-router'
import MainLayout from '@/layouts/MainLayout.vue'
import BlankLayout from '@/layouts/BlankLayout.vue'

export const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/login/index.vue'),
    meta: { title: '登录', requiresAuth: false }
  },
  {
    path: '/',
    component: MainLayout,
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/dashboard/index.vue'),
        meta: { title: '首页', icon: 'DashboardOutlined' }
      },
      // 基础配置
      {
        path: 'config',
        name: 'Config',
        redirect: '/config/currency',
        meta: { title: '基础配置', icon: 'SettingOutlined' },
        children: [
          {
            path: 'currency',
            name: 'Currency',
            component: () => import('@/views/config/currency/index.vue'),
            meta: { title: '货币管理', permission: 'config:currency' }
          },
          {
            path: 'marketplace',
            name: 'Marketplace',
            component: () => import('@/views/config/marketplace/index.vue'),
            meta: { title: '站点管理', permission: 'config:marketplace' }
          },
          {
            path: 'transaction-type',
            name: 'TransactionType',
            component: () => import('@/views/config/transactionType/index.vue'),
            meta: { title: '交易类型映射', permission: 'config:transactionType' }
          },
          {
            path: 'field-mapping',
            name: 'FieldMapping',
            component: () => import('@/views/config/fieldMapping/index.vue'),
            meta: { title: '字段映射模板', permission: 'config:fieldMapping' }
          },
          {
            path: 'import-record',
            name: 'ImportRecord',
            component: () => import('@/views/config/importRecord/index.vue'),
            meta: { title: '导入记录', permission: 'config:importRecord' }
          }
        ]
      },
      // 销售数据
      {
        path: 'sales',
        name: 'Sales',
        redirect: '/sales/list',
        meta: { title: '销售数据', icon: 'ShoppingCartOutlined' },
        children: [
          {
            path: 'import',
            name: 'SalesImport',
            component: () => import('@/views/sales/import/index.vue'),
            meta: { title: '数据导入', permission: 'sales:import' }
          },
          {
            path: 'list',
            name: 'SalesList',
            component: () => import('@/views/sales/list/index.vue'),
            meta: { title: '数据列表', permission: 'sales:list' }
          }
        ]
      },
      // 配送数据
      {
        path: 'shipping',
        name: 'Shipping',
        redirect: '/shipping/list',
        meta: { title: '配送数据', icon: 'CarOutlined' },
        children: [
          {
            path: 'import',
            name: 'ShippingImport',
            component: () => import('@/views/shipping/import/index.vue'),
            meta: { title: '数据导入', permission: 'shipping:import' }
          },
          {
            path: 'list',
            name: 'ShippingList',
            component: () => import('@/views/shipping/list/index.vue'),
            meta: { title: '数据列表', permission: 'shipping:list' }
          }
        ]
      },
      // 广告数据
      {
        path: 'advertising',
        name: 'Advertising',
        redirect: '/advertising/list',
        meta: { title: '广告数据', icon: 'NotificationOutlined' },
        children: [
          {
            path: 'add',
            name: 'AdvertisingAdd',
            component: () => import('@/views/advertising/add/index.vue'),
            meta: { title: '广告费录入', permission: 'advertising:add' }
          },
          {
            path: 'list',
            name: 'AdvertisingList',
            component: () => import('@/views/advertising/list/index.vue'),
            meta: { title: '数据列表', permission: 'advertising:list' }
          }
        ]
      },
      // 汇率管理
      {
        path: 'rate',
        name: 'Rate',
        redirect: '/rate/list',
        meta: { title: '汇率管理', icon: 'DollarOutlined' },
        children: [
          {
            path: 'import',
            name: 'RateImport',
            component: () => import('@/views/rate/import/index.vue'),
            meta: { title: '汇率导入', permission: 'rate:import' }
          },
          {
            path: 'list',
            name: 'RateList',
            component: () => import('@/views/rate/list/index.vue'),
            meta: { title: '汇率查询', permission: 'rate:list' }
          }
        ]
      },
      // 汇总报表
      {
        path: 'report',
        name: 'Report',
        redirect: '/report/summary',
        meta: { title: '汇总报表', icon: 'BarChartOutlined' },
        children: [
          {
            path: 'summary',
            name: 'ReportSummary',
            component: () => import('@/views/report/summary/index.vue'),
            meta: { title: '汇总查询', permission: 'report:summary' }
          },
          {
            path: 'download',
            name: 'ReportDownload',
            component: () => import('@/views/report/download/index.vue'),
            meta: { title: '报表下载', permission: 'report:download' }
          }
        ]
      },
      // 系统管理
      {
        path: 'system',
        name: 'System',
        redirect: '/system/user',
        meta: { title: '系统管理', icon: 'ToolOutlined', roles: ['admin'] },
        children: [
          {
            path: 'user',
            name: 'SystemUser',
            component: () => import('@/views/system/user/index.vue'),
            meta: { title: '用户管理', permission: 'system:user' }
          },
          {
            path: 'role',
            name: 'SystemRole',
            component: () => import('@/views/system/role/index.vue'),
            meta: { title: '权限管理', permission: 'system:role' }
          },
          {
            path: 'log',
            name: 'SystemLog',
            component: () => import('@/views/system/log/index.vue'),
            meta: { title: '操作日志', permission: 'system:log' }
          }
        ]
      }
    ]
  },
  // 404
  {
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: () => import('@/views/error/404.vue'),
    meta: { title: '页面不存在', requiresAuth: false }
  }
]
```

### 5.2 路由守卫

```typescript
// router/guards.ts
import type { Router } from 'vue-router'
import { useAuthStore } from '@/stores/modules/auth'
import { message } from 'ant-design-vue'

export function setupRouterGuards(router: Router) {
  // 前置守卫
  router.beforeEach(async (to, from, next) => {
    const authStore = useAuthStore()

    // 设置页面标题
    document.title = `${to.meta.title || ''} - 慕声报税系统`

    // 不需要认证的页面
    if (to.meta.requiresAuth === false) {
      next()
      return
    }

    // 未登录
    if (!authStore.isLoggedIn) {
      next({ name: 'Login', query: { redirect: to.fullPath } })
      return
    }

    // 权限检查
    if (to.meta.permission && !authStore.hasPermission(to.meta.permission as string)) {
      message.error('没有访问权限')
      next({ name: 'Dashboard' })
      return
    }

    // 角色检查
    if (to.meta.roles && !(to.meta.roles as string[]).includes(authStore.userInfo?.role || '')) {
      message.error('没有访问权限')
      next({ name: 'Dashboard' })
      return
    }

    next()
  })

  // 后置守卫
  router.afterEach((to) => {
    // 滚动到顶部
    window.scrollTo(0, 0)
  })
}
```

---

## 6. 接口对接

### 6.1 Axios封装

```typescript
// utils/request.ts
import axios, { type AxiosInstance, type AxiosRequestConfig, type AxiosResponse } from 'axios'
import { message } from 'ant-design-vue'
import { useAuthStore } from '@/stores/modules/auth'
import { getToken } from '@/utils/auth'
import router from '@/router'

// 创建实例
const service: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
service.interceptors.request.use(
  (config) => {
    const token = getToken()
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
service.interceptors.response.use(
  (response: AxiosResponse) => {
    const res = response.data

    // 文件下载
    if (response.config.responseType === 'blob') {
      return response
    }

    // 业务状态码判断
    if (res.code !== 0) {
      message.error(res.message || '请求失败')

      // Token过期
      if (res.code === 401) {
        const authStore = useAuthStore()
        authStore.logoutAction()
        router.push('/login')
      }

      return Promise.reject(new Error(res.message || '请求失败'))
    }

    return res
  },
  (error) => {
    // 网络错误处理
    if (error.response) {
      switch (error.response.status) {
        case 401:
          message.error('登录已过期，请重新登录')
          const authStore = useAuthStore()
          authStore.logoutAction()
          router.push('/login')
          break
        case 403:
          message.error('没有权限访问')
          break
        case 404:
          message.error('请求的资源不存在')
          break
        case 500:
          message.error('服务器内部错误')
          break
        default:
          message.error(error.response.data?.message || '请求失败')
      }
    } else if (error.code === 'ECONNABORTED') {
      message.error('请求超时，请稍后重试')
    } else {
      message.error('网络异常，请检查网络连接')
    }

    return Promise.reject(error)
  }
)

// 封装请求方法
export const request = {
  get<T = any>(url: string, config?: AxiosRequestConfig): Promise<T> {
    return service.get(url, config)
  },
  post<T = any>(url: string, data?: any, config?: AxiosRequestConfig): Promise<T> {
    return service.post(url, data, config)
  },
  put<T = any>(url: string, data?: any, config?: AxiosRequestConfig): Promise<T> {
    return service.put(url, data, config)
  },
  delete<T = any>(url: string, config?: AxiosRequestConfig): Promise<T> {
    return service.delete(url, config)
  },
  upload<T = any>(url: string, file: File, config?: AxiosRequestConfig): Promise<T> {
    const formData = new FormData()
    formData.append('file', file)
    return service.post(url, formData, {
      ...config,
      headers: { 'Content-Type': 'multipart/form-data' }
    })
  },
  download(url: string, params?: any): Promise<AxiosResponse<Blob>> {
    return service.get(url, {
      params,
      responseType: 'blob'
    })
  }
}

export default service
```

### 6.2 API接口列表

#### 6.2.1 认证接口

```typescript
// api/auth.ts
import { request } from '@/utils/request'
import type { LoginParams, LoginResult, UserInfo } from '@/types/auth'

/** 用户登录 */
export function login(data: LoginParams): Promise<LoginResult> {
  return request.post('/api/v1/auth/login', data)
}

/** 用户登出 */
export function logout(): Promise<void> {
  return request.post('/api/v1/auth/logout')
}

/** 刷新Token */
export function refreshToken(): Promise<{ token: string }> {
  return request.post('/api/v1/auth/refresh')
}

/** 获取当前用户信息 */
export function getCurrentUser(): Promise<UserInfo> {
  return request.get('/api/v1/auth/current-user')
}
```

#### 6.2.2 销售数据接口

```typescript
// api/sales.ts
import { request } from '@/utils/request'
import type { SalesData, SalesQuery, PageResult, ImportResult } from '@/types/sales'

/** 查询销售数据列表 */
export function getSalesList(params: SalesQuery): Promise<PageResult<SalesData>> {
  return request.get('/api/v1/sales', { params })
}

/** 导入销售数据 */
export function importSales(file: File, templateId: number): Promise<ImportResult> {
  const formData = new FormData()
  formData.append('file', file)
  formData.append('templateId', String(templateId))
  return request.post('/api/v1/sales/import', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}

/** 解析CSV表头 */
export function parseCSVHeaders(file: File): Promise<{ headers: string[]; samples: string[] }> {
  const formData = new FormData()
  formData.append('file', file)
  return request.post('/api/v1/sales/parse-headers', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
  })
}

/** 导出销售数据 */
export function exportSales(params: SalesQuery): Promise<Blob> {
  return request.download('/api/v1/sales/export', params)
}

/** 按批次删除销售数据 */
export function deleteSalesByBatch(batchId: number): Promise<void> {
  return request.delete(`/api/v1/sales/batch/${batchId}`)
}
```

#### 6.2.3 字段映射模板接口

```typescript
// api/fieldMapping.ts
import { request } from '@/utils/request'
import type { MappingTemplate, MappingConfig } from '@/types/fieldMapping'

/** 获取映射模板列表 */
export function getMappingTemplates(siteCode?: string): Promise<MappingTemplate[]> {
  return request.get('/api/v1/field-mapping/templates', { params: { siteCode } })
}

/** 获取映射模板详情 */
export function getMappingTemplateById(id: number): Promise<MappingTemplate> {
  return request.get(`/api/v1/field-mapping/templates/${id}`)
}

/** 保存映射模板 */
export function saveMappingTemplate(data: Partial<MappingTemplate>): Promise<MappingTemplate> {
  return request.post('/api/v1/field-mapping/templates', data)
}

/** 更新映射模板 */
export function updateMappingTemplate(id: number, data: Partial<MappingTemplate>): Promise<MappingTemplate> {
  return request.put(`/api/v1/field-mapping/templates/${id}`, data)
}

/** 删除映射模板 */
export function deleteMappingTemplate(id: number): Promise<void> {
  return request.delete(`/api/v1/field-mapping/templates/${id}`)
}

/** 设置默认模板 */
export function setDefaultTemplate(id: number): Promise<void> {
  return request.put(`/api/v1/field-mapping/templates/${id}/default`)
}

/** 获取站点默认模板 */
export function getDefaultTemplate(siteCode: string): Promise<MappingTemplate | null> {
  return request.get(`/api/v1/field-mapping/templates/default/${siteCode}`)
}
```

#### 6.2.4 汇总报表接口

```typescript
// api/report.ts
import { request } from '@/utils/request'
import type { ReportQuery, ReportSummary, ReportDetail } from '@/types/report'

/** 查询汇总报表 */
export function getReportSummary(params: ReportQuery): Promise<ReportSummary[]> {
  return request.get('/api/v1/report/summary', { params })
}

/** 查询汇总明细 */
export function getReportDetail(params: ReportQuery): Promise<ReportDetail[]> {
  return request.get('/api/v1/report/detail', { params })
}

/** 导出报表 */
export function exportReport(params: ReportQuery): Promise<Blob> {
  return request.download('/api/v1/report/export', params)
}

/** 数据校验 */
export function validateReportData(params: ReportQuery): Promise<{
  valid: boolean
  missingRates: string[]
  missingData: string[]
}> {
  return request.get('/api/v1/report/validate', { params })
}
```

---

## 7. 性能方案

### 7.1 大文件处理方案

#### 7.1.1 分片上传

```typescript
// 分片上传配置
const CHUNK_SIZE = 2 * 1024 * 1024 // 2MB
const MAX_CONCURRENT = 3 // 最大并发数

// 流程:
// 1. 前端计算文件MD5
// 2. 检查是否已上传（秒传）
// 3. 分片上传
// 4. 服务端合并
```

#### 7.1.2 前端CSV解析优化

```typescript
// utils/csv.ts
import Papa from 'papaparse'

/**
 * 解析CSV文件表头（只读取前几行）
 */
export async function parseCSVHeaders(
  file: File,
  maxRows: number = 10
): Promise<{ headers: string[]; samples: string[][] }> {
  return new Promise((resolve, reject) => {
    let rowCount = 0
    const results: string[][] = []

    Papa.parse(file, {
      step: (row, parser) => {
        results.push(row.data as string[])
        rowCount++
        if (rowCount >= maxRows) {
          parser.abort()
        }
      },
      complete: () => {
        // 跳过说明行，找到表头行
        const headerIndex = findHeaderRow(results)
        const headers = results[headerIndex] || []
        const samples = results.slice(headerIndex + 1)
        resolve({ headers, samples })
      },
      error: reject
    })
  })
}

/**
 * 查找表头行（包含关键字段的行）
 */
function findHeaderRow(rows: string[][]): number {
  const keyFields = ['date/time', 'Datum/Uhrzeit', 'order id', 'Bestellnummer']

  for (let i = 0; i < rows.length; i++) {
    const row = rows[i].map(cell => cell.toLowerCase())
    if (keyFields.some(key => row.includes(key.toLowerCase()))) {
      return i
    }
  }
  return 0
}
```

### 7.2 虚拟滚动方案

#### 7.2.1 vxe-table配置

```typescript
// 虚拟滚动配置
const tableConfig = {
  // 启用Y轴虚拟滚动，数据超过100条时生效
  scrollY: {
    enabled: true,
    gt: 100,
    oSize: 20 // 渲染缓冲区大小
  },
  // 启用X轴虚拟滚动
  scrollX: {
    enabled: true,
    gt: 20
  },
  // 行配置
  rowConfig: {
    isHover: true,
    keyField: 'id',
    height: 48 // 固定行高，提升性能
  }
}
```

#### 7.2.2 性能优化要点

1. **固定行高**: 设置固定行高避免动态计算
2. **减少列数**: 默认只显示核心列，支持列配置
3. **分页加载**: 配合后端分页，每次加载500-1000条
4. **防抖筛选**: 筛选输入防抖300ms

### 7.3 懒加载方案

#### 7.3.1 路由懒加载

```typescript
// 所有页面组件使用动态导入
const routes = [
  {
    path: '/sales/list',
    component: () => import('@/views/sales/list/index.vue')
  }
]
```

#### 7.3.2 组件懒加载

```vue
<script setup lang="ts">
import { defineAsyncComponent } from 'vue'

// 大型组件异步加载
const FieldMappingCanvas = defineAsyncComponent(
  () => import('@/components/business/FieldMappingCanvas/index.vue')
)

const ECharts = defineAsyncComponent(
  () => import('@/components/common/ECharts/index.vue')
)
</script>
```

#### 7.3.3 图片懒加载

```vue
<template>
  <img v-lazy="imageUrl" alt="" />
</template>

<script setup lang="ts">
// 使用 vueuse 的 useIntersectionObserver
import { useIntersectionObserver } from '@vueuse/core'
</script>
```

### 7.4 缓存策略

```typescript
// 数据缓存策略
const cacheConfig = {
  // 货币列表 - 长期缓存
  currency: { ttl: 24 * 60 * 60 * 1000 }, // 24小时

  // 站点列表 - 长期缓存
  marketplace: { ttl: 24 * 60 * 60 * 1000 }, // 24小时

  // 映射模板 - 中期缓存
  mappingTemplate: { ttl: 60 * 60 * 1000 }, // 1小时

  // 汇率数据 - 短期缓存
  exchangeRate: { ttl: 10 * 60 * 1000 } // 10分钟
}
```

---

## 8. 兼容性方案

### 8.1 浏览器兼容

#### 8.1.1 Vite配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import legacy from '@vitejs/plugin-legacy'

export default defineConfig({
  plugins: [
    vue(),
    legacy({
      targets: ['Chrome >= 90', 'Edge >= 90', 'Firefox >= 90', 'Safari >= 14'],
      additionalLegacyPolyfills: ['regenerator-runtime/runtime']
    })
  ],
  build: {
    target: 'es2015'
  }
})
```

#### 8.1.2 CSS兼容

```scss
// 使用autoprefixer自动添加前缀
// postcss.config.js
module.exports = {
  plugins: {
    autoprefixer: {
      overrideBrowserslist: [
        'Chrome >= 90',
        'Edge >= 90',
        'Firefox >= 90',
        'Safari >= 14'
      ]
    }
  }
}
```

### 8.2 响应式适配

```scss
// assets/styles/variables.scss

// 断点定义
$breakpoints: (
  'sm': 576px,
  'md': 768px,
  'lg': 992px,
  'xl': 1200px,
  'xxl': 1600px
);

// 最小支持宽度
$min-width: 1280px;

// 响应式mixin
@mixin respond-to($breakpoint) {
  @if map-has-key($breakpoints, $breakpoint) {
    @media (min-width: map-get($breakpoints, $breakpoint)) {
      @content;
    }
  }
}
```

### 8.3 功能检测

```typescript
// utils/feature.ts

/** 检测浏览器是否支持必要功能 */
export function checkBrowserSupport(): { supported: boolean; missing: string[] } {
  const missing: string[] = []

  // 检测 File API
  if (!window.File || !window.FileReader || !window.FileList || !window.Blob) {
    missing.push('File API')
  }

  // 检测 FormData
  if (!window.FormData) {
    missing.push('FormData')
  }

  // 检测 Fetch
  if (!window.fetch) {
    missing.push('Fetch API')
  }

  // 检测 Proxy
  if (typeof Proxy === 'undefined') {
    missing.push('Proxy')
  }

  return {
    supported: missing.length === 0,
    missing
  }
}
```

---

## 9. 开发规范

### 9.1 Git提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式（不影响代码运行）
refactor: 重构
perf: 性能优化
test: 测试相关
chore: 构建/工具相关
```

### 9.2 代码审查清单

- [ ] TypeScript类型是否完整
- [ ] 组件Props是否有默认值
- [ ] 事件命名是否符合规范
- [ ] 是否有console.log未删除
- [ ] 是否有未使用的变量/导入
- [ ] 样式是否使用scoped
- [ ] 大文件是否需要懒加载
- [ ] API错误是否有处理

---

## 10. 附录

### 10.1 系统目标字段定义（供映射画布使用）

| 字段名 | 显示标签 | 描述 | 必填 | 默认值 | 数据类型 |
|--------|---------|------|------|--------|---------|
| transaction_date | 交易日期时间 | 交易发生的日期和时间 | 是 | - | datetime |
| settlement_id | 结算编号 | 亚马逊结算批次编号 | 否 | 空串 | string |
| transaction_type | 交易类型 | 原始交易类型值 | 是 | - | string |
| order_id | 订单号 | 亚马逊订单编号 | 是 | - | string |
| sku | SKU | 商品SKU编码 | 否 | 空串 | string |
| description | 商品描述 | 商品描述信息 | 否 | 空串 | string |
| quantity | 数量 | 交易数量 | 否 | 0 | number |
| marketplace | 站点标识 | 原始marketplace值 | 是 | - | string |
| account_type | 账户类型 | 账户类型（仅US有） | 否 | 空串 | string |
| fulfillment | 配送方式 | FBA/MFN | 否 | 空串 | string |
| order_city | 订单城市 | 订单收货城市 | 否 | 空串 | string |
| order_state | 订单省/州 | 订单收货省/州 | 否 | 空串 | string |
| order_postal | 订单邮编 | 订单收货邮编 | 否 | 空串 | string |
| tax_collection_model | 税收模式 | 税收收取模式 | 否 | 空串 | string |
| product_sales | 产品销售额 | 产品销售金额 | 是 | 0 | number |
| product_sales_tax | 产品税 | 产品税金额 | 否 | 0 | number |
| shipping_credits | 运费收入 | 运费收入金额 | 否 | 0 | number |
| shipping_credits_tax | 运费税 | 运费税金额 | 否 | 0 | number |
| gift_wrap_credits | 礼品包装费 | 礼品包装费金额 | 否 | 0 | number |
| gift_wrap_credits_tax | 礼品包装税 | 礼品包装税金额 | 否 | 0 | number |
| regulatory_fee | 监管费 | 监管费金额（UK/DE无） | 否 | 0 | number |
| regulatory_fee_tax | 监管费税 | 监管费税金额（UK/DE无） | 否 | 0 | number |
| promotional_rebates | 促销折扣 | 促销折扣金额 | 否 | 0 | number |
| promotional_rebates_tax | 促销折扣税 | 促销折扣税金额 | 否 | 0 | number |
| marketplace_withheld_tax | 平台代扣税 | 平台代扣税金额 | 否 | 0 | number |
| selling_fees | 销售费用 | 亚马逊销售费用 | 否 | 0 | number |
| fba_fees | FBA费用 | FBA配送费用 | 否 | 0 | number |
| other_transaction_fees | 其他交易费 | 其他交易费用 | 否 | 0 | number |
| other | 其他 | 其他金额 | 否 | 0 | number |
| total | 合计 | 合计金额 | 否 | 0 | number |

### 10.2 技术风险及应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 10万行数据渲染卡顿 | 高 | 使用vxe-table虚拟滚动 + 分页加载 |
| 大文件上传失败 | 高 | 分片上传 + 断点续传 + 重试机制 |
| CSV解析阻塞页面 | 中 | Web Worker异步解析 |
| 字段映射连线性能 | 中 | SVG优化 + 防抖重绘 |
| 多语言字段展示 | 低 | Tooltip展示完整内容 |

---

*文档创建时间：2026年1月19日*
*版本：v1.0*
