# 报税汇总改造设计文档

> 基于新公式（`docs/报税汇总计算公式.md`）对 `TaxReportServiceImpl` 进行全面改造

---

## 一、改造范围总览

| 改造项 | 涉及文件 | 改造类型 |
|-------|---------|---------|
| 退款筛选口径 | TaxReportServiceImpl.java | 逻辑重写 |
| 退款费用项（9项） | TaxReportServiceImpl.java | 方法修改 |
| 退款冗余维度清理 | TaxReportServiceImpl.java + TaxReportSummary.java | 删除代码+字段 |
| 方式一佣金服务费归类 | TaxReportServiceImpl.java | 逻辑调整 |
| 方式二费用取值 | TaxReportServiceImpl.java | 逻辑重写 |
| 方式二平台代扣税 | TaxReportServiceImpl.java | 新增逻辑 |
| 平台支出公式联动 | TaxReportServiceImpl.java | 公式调整 |
| 前端字段对接 | index.vue | 字段名替换 |
| 导出 Excel | TaxReportServiceImpl.java | 字段替换 |
| hasAnyTaxData 判断 | TaxReportServiceImpl.java | 字段替换 |

---

## 二、退款改造（核心变更）

### 2.1 筛选口径：从交易日期改为配送日期

改造前（Amazon 口径，按交易日期）：
```java
// 旧：按交易日期在季度内，不关联配送数据
List<SalesData> refundListAmazon = allSalesData.stream()
    .filter(s -> "refund".equals(s.getTransactionCategory()))
    .filter(s -> {
        LocalDate transDate = s.getTransactionDate().toLocalDate();
        return !transDate.isBefore(startDate) && !transDate.isAfter(endDate);
    })
    .collect(Collectors.toList());
```

改造后（配送日期口径）：
```java
// 新：通过订单号关联配送数据，配送日期在季度内
List<SalesData> refundList = allSalesData.stream()
    .filter(s -> "refund".equals(s.getTransactionCategory()))
    .filter(s -> s.getOrderId() != null && shippingOrderIds.contains(s.getOrderId()))
    .collect(Collectors.toList());
```

关键点：
- `shippingOrderIds` 是本季度配送日期在季度内的订单号集合（已有，无需新建）
- 退款数据通过 orderId 关联到配送数据，配送日期决定归属季度
- 汇率使用 `orderRateMap.get(orderId)`，即配送日期汇率

### 2.2 退款费用项：从 8 项改为 9 项

改造前（`calculateRefundAmount` 方法）：
```java
// 旧：8项，含 promotionalRebatesTax，不含 regulatoryFee/regulatoryFeeTax
private BigDecimal calculateRefundAmount(SalesData refund) {
    return nullToZero(refund.getProductSales())
        .add(nullToZero(refund.getProductSalesTax()))
        .add(nullToZero(refund.getShippingCredits()))
        .add(nullToZero(refund.getShippingCreditsTax()))
        .add(nullToZero(refund.getGiftWrapCredits()))
        .add(nullToZero(refund.getGiftWrapCreditsTax()))
        .add(nullToZero(refund.getPromotionalRebates()))
        .add(nullToZero(refund.getPromotionalRebatesTax()));
}
```

改造后：
```java
/**
 * 计算退款金额（9项）
 * 公式：产品销售额 + 产品税 + 运费收入 + 运费税 + 礼品包装收入 + 礼品包装税
 *      + 监管费 + 监管费税 + 促销折扣
 *
 * @param refund 退款记录
 * @return 退款小计
 * @author wanhua
 * 10:30 2026年03月20日
 */
private BigDecimal calculateRefundAmount(SalesData refund) {
    return nullToZero(refund.getProductSales())
        .add(nullToZero(refund.getProductSalesTax()))
        .add(nullToZero(refund.getShippingCredits()))
        .add(nullToZero(refund.getShippingCreditsTax()))
        .add(nullToZero(refund.getGiftWrapCredits()))
        .add(nullToZero(refund.getGiftWrapCreditsTax()))
        .add(nullToZero(refund.getRegulatoryFee()))
        .add(nullToZero(refund.getRegulatoryFeeTax()))
        .add(nullToZero(refund.getPromotionalRebates()));
}
```

变更点：
- ✅ 新增 `regulatoryFee`（监管费）
- ✅ 新增 `regulatoryFeeTax`（监管费税）
- ❌ 移除 `promotionalRebatesTax`（促销折扣税）

### 2.3 退款冗余维度清理

删除以下三个维度，统一为单一配送日期口径：

| 删除维度 | 涉及变量 | 涉及 DTO 字段 |
|---------|---------|-------------|
| 按结算时间 | refundBySettlement / refundBySettlementCny / refundCountBySettlement | refundBySettlement, refundBySettlementCny, refundCountBySettlement |
| 按发货归属（旧） | refundByShipment / refundByShipmentCny / refundCountByShipment | refundByShipment, refundByShipmentCny, refundCountByShipment |
| Amazon口径 | refundBySettlementAmazon / refundBySettlementAmazonCny / refundCountBySettlementAmazon | refundBySettlementAmazon, refundBySettlementAmazonCny, refundCountBySettlementAmazon |

替换为单一字段：

| 新字段 | 说明 |
|-------|------|
| refundAmount | 退款金额（原币） |
| refundAmountCny | 退款金额（人民币） |
| refundCount | 退款笔数 |

### 2.4 退款计算改造后完整逻辑

```java
// ========== 退款计算（单一配送日期口径） ==========
BigDecimal refundAmount = BigDecimal.ZERO;
BigDecimal refundAmountCny = BigDecimal.ZERO;
int refundCount = 0;

// 退款相关费用（佣金/其他/税）
BigDecimal refundConsumptionTax = BigDecimal.ZERO;
BigDecimal refundConsumptionTaxCny = BigDecimal.ZERO;
BigDecimal refundSellingFees = BigDecimal.ZERO;
BigDecimal refundSellingFeesCny = BigDecimal.ZERO;
BigDecimal refundFbaFees = BigDecimal.ZERO;
BigDecimal refundFbaFeesCny = BigDecimal.ZERO;
BigDecimal refundOtherTransFees = BigDecimal.ZERO;
BigDecimal refundOtherTransFeesCny = BigDecimal.ZERO;
BigDecimal refundOther = BigDecimal.ZERO;
BigDecimal refundOtherCny = BigDecimal.ZERO;

// 筛选：refund 类型 + 订单号在本季度配送订单集合中
List<SalesData> refundList = allSalesData.stream()
    .filter(s -> "refund".equals(s.getTransactionCategory()))
    .filter(s -> s.getOrderId() != null && shippingOrderIds.contains(s.getOrderId()))
    .collect(Collectors.toList());

for (SalesData refund : refundList) {
    BigDecimal amt = calculateRefundAmount(refund);
    // 汇率：配送日期汇率
    BigDecimal rate = orderRateMap.getOrDefault(refund.getOrderId(), BigDecimal.ONE);
    if (rate.compareTo(BigDecimal.ZERO) <= 0) rate = BigDecimal.ONE;

    refundAmount = refundAmount.add(amt);
    refundAmountCny = refundAmountCny.add(amt.multiply(rate));
    refundCount++;

    // 退款关联的费用（佣金/其他/税），同样使用配送日期汇率
    BigDecimal tax = nullToZero(refund.getMarketplaceWithheldTax());
    refundConsumptionTax = refundConsumptionTax.add(tax);
    refundConsumptionTaxCny = refundConsumptionTaxCny.add(tax.multiply(rate));

    BigDecimal selling = nullToZero(refund.getSellingFees());
    refundSellingFees = refundSellingFees.add(selling);
    refundSellingFeesCny = refundSellingFeesCny.add(selling.multiply(rate));

    BigDecimal fba = nullToZero(refund.getFbaFees());
    refundFbaFees = refundFbaFees.add(fba);
    refundFbaFeesCny = refundFbaFeesCny.add(fba.multiply(rate));

    BigDecimal otherTrans = nullToZero(refund.getOtherTransactionFees());
    refundOtherTransFees = refundOtherTransFees.add(otherTrans);
    refundOtherTransFeesCny = refundOtherTransFeesCny.add(otherTrans.multiply(rate));

    BigDecimal other = nullToZero(refund.getOther());
    refundOther = refundOther.add(other);
    refundOtherCny = refundOtherCny.add(other.multiply(rate));
}
```

---

## 三、平台扣费改造

### 3.1 方式一：佣金服务费从 4 项改为 3 项，other 拆出

改造前：
```java
// 旧：佣金合计 = sellingFees + fbaFees + otherTransactionFees + other（4项）
BigDecimal totalServiceFee = sellingFees.add(fbaFees).add(otherTransactionFees).add(otherAmount);
```

改造后：
```java
// 新：佣金服务费 = sellingFees + fbaFees + otherTransactionFees（3项）
BigDecimal commissionFee = sellingFees.add(fbaFees).add(otherTransactionFees);
BigDecimal commissionFeeCny = sellingFeesCny.add(fbaFeesCny).add(otherTransactionFeesCny);

// other 单独作为"其他费"（方式一部分）
// otherAmount / otherAmountCny 已有，直接使用
```

### 3.2 方式二：从取 total 改为取具体费用项

改造前：
```java
// 旧：取 total 字段，按 transactionType 拆分
BigDecimal amount = nullToZero(other.getTotal());
if (isServiceFee) {
    miscServiceFee = miscServiceFee.add(amount);
} else {
    otherFees = otherFees.add(amount);
}
```

改造后：
```java
// 新：取具体费用项，按费用类别归入
for (SalesData other : allOtherData) {
    if (other.getTransactionDate() == null) continue;
    LocalDate transDate = other.getTransactionDate().toLocalDate();
    if (transDate.isBefore(startDate) || transDate.isAfter(endDate)) continue;
    if ("Transfer".equalsIgnoreCase(other.getTransactionType())) continue;

    BigDecimal rate = other.getExchangeRate();
    if (rate == null || rate.compareTo(BigDecimal.ZERO) <= 0) rate = BigDecimal.ONE;

    // 佣金服务费（3项）
    BigDecimal selling = nullToZero(other.getSellingFees());
    BigDecimal fba = nullToZero(other.getFbaFees());
    BigDecimal otherTrans = nullToZero(other.getOtherTransactionFees());
    miscCommissionFee = miscCommissionFee.add(selling).add(fba).add(otherTrans);
    miscCommissionFeeCny = miscCommissionFeeCny
        .add(selling.multiply(rate)).add(fba.multiply(rate)).add(otherTrans.multiply(rate));

    // 其他费
    BigDecimal otherAmt = nullToZero(other.getOther());
    miscOtherFee = miscOtherFee.add(otherAmt);
    miscOtherFeeCny = miscOtherFeeCny.add(otherAmt.multiply(rate));

    // 平台代扣税（新增：方式二也统计税）
    BigDecimal tax = nullToZero(other.getMarketplaceWithheldTax());
    miscConsumptionTax = miscConsumptionTax.add(tax);
    miscConsumptionTaxCny = miscConsumptionTaxCny.add(tax.multiply(rate));
}
```

### 3.3 费用汇总公式调整

```java
// ========== 费用汇总（方式一 + 方式二） ==========

// 佣金服务费合计 = 方式一(income+refund的3项) + 方式二(其他类型的3项)
BigDecimal totalCommissionFee = commissionFee.add(miscCommissionFee);
BigDecimal totalCommissionFeeCny = commissionFeeCny.add(miscCommissionFeeCny);

// 其他费合计 = 方式一(income+refund的other) + 方式二(其他类型的other)
BigDecimal totalOtherFee = otherAmount.add(miscOtherFee);
BigDecimal totalOtherFeeCny = otherAmountCny.add(miscOtherFeeCny);

// 税合计 = 方式一(income+refund的marketplaceWithheldTax) + 方式二(其他类型的marketplaceWithheldTax)
BigDecimal totalConsumptionTax = consumptionTax.add(miscConsumptionTax);
BigDecimal totalConsumptionTaxCny = consumptionTaxCny.add(miscConsumptionTaxCny);
```

---

## 四、平台支出与采购成本联动

```java
// ③收入净额 = 收入总额 - |退款金额|
BigDecimal netRevenueCny = totalRevenueCny.subtract(refundAmountCny.abs());

// ④税 = totalConsumptionTaxCny（方式一+方式二）
// ⑤佣金服务费 = totalCommissionFeeCny（方式一+方式二）
// ⑥广告费 = advertisingCostCny
// ⑦其他费 = totalOtherFeeCny（方式一+方式二）
// ⑨平台支出合计 = |④| + |⑤| + |⑥| + |⑦|
BigDecimal platformExpensesCny = totalConsumptionTaxCny.abs()
    .add(totalCommissionFeeCny.abs())
    .add(advertisingCostCny.abs())
    .add(totalOtherFeeCny.abs());

// ⑩4%利润 = ③ × 4%
BigDecimal profit4PercentCny = netRevenueCny.multiply(new BigDecimal("0.04"));

// ⑪采购成本 = ③ − ⑨ − ⑩
BigDecimal procurementCostCny = netRevenueCny.subtract(platformExpensesCny).subtract(profit4PercentCny);
```

---

## 五、TaxReportSummary DTO 改造

### 5.1 删除字段（冗余退款维度 + 旧佣金字段）

```java
// 删除：退款三维度
private BigDecimal refundByShipment;
private BigDecimal refundByShipmentCny;
private Integer refundCountByShipment;
private BigDecimal refundBySettlement;
private BigDecimal refundBySettlementCny;
private Integer refundCountBySettlement;
private BigDecimal refundBySettlementAmazon;
private BigDecimal refundBySettlementAmazonCny;
private Integer refundCountBySettlementAmazon;

// 删除：旧佣金合计（含other的4项合计）
private BigDecimal totalServiceFee;
private BigDecimal totalServiceFeeCny;

// 删除：旧的方式二拆分字段（按transactionType拆分）
private BigDecimal miscServiceFee;
private BigDecimal miscServiceFeeCny;
private BigDecimal otherFees;
private BigDecimal otherFeesCny;
private Integer miscFeesCount;
private BigDecimal totalMiscFees;
private BigDecimal totalMiscFeesCny;

// 删除：旧总成本
private BigDecimal totalCost;
```

### 5.2 新增/保留字段

```java
// ========== 退款（单一口径） ==========
@Schema(description = "退款金额（原币）- 配送日期口径")
private BigDecimal refundAmount;

@Schema(description = "退款金额（人民币）- 配送日期口径")
private BigDecimal refundAmountCny;

@Schema(description = "退款笔数")
private Integer refundCount;

// ========== 佣金服务费（方式一+方式二合计，3项） ==========
@Schema(description = "佣金服务费合计（原币）= sellingFees + fbaFees + otherTransactionFees")
private BigDecimal totalCommissionFee;

@Schema(description = "佣金服务费合计（人民币）")
private BigDecimal totalCommissionFeeCny;

// ========== 其他费（方式一+方式二合计） ==========
@Schema(description = "其他费合计（原币）= income/refund的other + 其他类型的other")
private BigDecimal totalOtherFee;

@Schema(description = "其他费合计（人民币）")
private BigDecimal totalOtherFeeCny;

// ========== 税（方式一+方式二合计） ==========
// consumptionTax / consumptionTaxCny 保留，含义扩大为方式一+方式二
```

### 5.3 保留不变的字段

```
totalRevenue / totalRevenueCny          — 收入
sellingFees / sellingFeesCny            — 销售费用明细（方式一）
fbaFees / fbaFeesCny                    — FBA费用明细（方式一）
otherTransactionFees / otherTransactionFeesCny — 其他交易费明细（方式一）
otherAmount / otherAmountCny            — 其他明细（方式一）
consumptionTax / consumptionTaxCny      — 税（含义扩大）
advertisingCost / advertisingCostCny    — 广告费
platformExpenses / platformExpensesCny  — 平台支出合计
profit4Percent / profit4PercentCny      — 4%利润
procurementCost / procurementCostCny    — 采购成本
siteCode / siteName / yearQuarter / currencyCode — 基础信息
```

---

## 六、前端联动改造

### 6.1 字段替换

| 旧字段 | 新字段 | 位置 |
|-------|-------|------|
| refundBySettlementAmazonCny | refundAmountCny | index.vue 全部引用 |
| refundCountBySettlementAmazon | refundCount | index.vue 退款笔数 |
| totalCommissionFeeCny | totalCommissionFeeCny | 不变（字段名相同，含义从旧口径改为方式一+方式二） |
| totalMiscFeesCny | totalOtherFeeCny | index.vue 其他费用列 |

### 6.2 导出 Excel 改造

```java
// 退款列：从 refundBySettlementAmazonCny 改为 refundAmountCny
row.createCell(col++).setCellValue(toDouble(s.getRefundAmountCny()));

// 其他费用列：从 totalMiscFeesCny 改为 totalOtherFeeCny
row.createCell(col++).setCellValue(toDouble(s.getTotalOtherFeeCny()));
```

---

## 七、hasAnyTaxData 判断改造

```java
private static boolean hasAnyTaxData(TaxReportSummary s) {
    return isNonZero(s.getTotalRevenueCny()) || isNonZero(s.getTotalRevenue())
        || (s.getRefundCount() != null && s.getRefundCount() > 0)
        || isNonZero(s.getRefundAmountCny())
        || isNonZero(s.getTotalCommissionFeeCny()) || isNonZero(s.getConsumptionTaxCny())
        || isNonZero(s.getAdvertisingCostCny()) || isNonZero(s.getPlatformExpensesCny())
        || isNonZero(s.getProfit4PercentCny());
}
```

---

## 八、数据流闭环验证

### 8.1 收入 → 退款 → 净额 闭环

```
收入总额 = 配送表 8 项 × 发货日期汇率
退款金额 = 结算报告 refund 9 项 × 配送日期汇率（通过 orderId 关联，配送日期在季度内）
收入净额 = 收入总额 − |退款金额|
```

✅ 收入和退款都通过配送日期归属季度，口径一致。

### 8.2 费用三类 → 平台支出 闭环

```
佣金服务费 = 方式一(income+refund 的 3 项 × 配送汇率) + 方式二(其他类型的 3 项 × 交易日期汇率)
其他费     = 方式一(income+refund 的 other × 配送汇率) + 方式二(其他类型的 other × 交易日期汇率)
税         = 方式一(income+refund 的 marketplaceWithheldTax × 配送汇率) + 方式二(其他类型的 marketplaceWithheldTax × 交易日期汇率)

平台支出合计 = |佣金服务费| + |其他费| + |税| + |广告费|
```

✅ 三类费用都按方式一+方式二叠加，逻辑对称。
✅ 方式二新增了 marketplaceWithheldTax 统计，不再遗漏。
✅ other 从佣金中拆出，归类正确。

### 8.3 采购成本 闭环

```
采购成本 = 收入净额 − 平台支出合计 − 4%利润
```

✅ 所有中间变量都已按新公式计算，最终结果自洽。

---

## 九、专家核验清单

| 核验项 | 状态 | 说明 |
|-------|------|------|
| 退款筛选口径统一为配送日期 | ✅ | 通过 orderId 关联 shippingOrderIds |
| 退款费用项 9 项正确 | ✅ | +regulatoryFee +regulatoryFeeTax -promotionalRebatesTax |
| SalesData 实体有 regulatoryFee/regulatoryFeeTax 字段 | ✅ | 已确认存在 |
| 退款冗余维度全部清理 | ✅ | 3 个旧维度 → 1 个新维度 |
| 方式一佣金 3 项，other 拆出 | ✅ | sellingFees+fbaFees+otherTransactionFees |
| 方式二从 total 改为具体费用项 | ✅ | 5 个字段分别归入 3 类 |
| 方式二新增 marketplaceWithheldTax | ✅ | 税的统计范围扩大到所有非 Transfer 类型 |
| 平台支出公式联动正确 | ✅ | 使用新的三类费用合计 |
| 收入净额使用新退款字段 | ✅ | refundAmountCny 替代 refundBySettlementAmazonCny |
| 前端字段对接明确 | ✅ | 4 处字段替换已列出 |
| 导出 Excel 联动 | ✅ | 退款列和其他费用列已更新 |
| hasAnyTaxData 联动 | ✅ | 使用新字段判断 |
| 汇率规则一致 | ✅ | income/refund 用配送汇率，其他类型用交易日期汇率 |
| 不影响广告费计算 | ✅ | 广告费逻辑不变 |
| 不影响收入计算 | ✅ | 收入逻辑不变 |
