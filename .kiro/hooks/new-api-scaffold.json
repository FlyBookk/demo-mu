{
  "name": "New API Scaffold",
  "version": "1.0.0",
  "description": "手动触发：根据 QuickCEP 规范创建新的 RESTful API 类",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "请根据 QuickCEP 研发规范创建一个新的 RESTful API 类。需要包含：\n1. @RestController, @Slf4j, @CrossOrigin, @Api 注解\n2. 标准的 CRUD 方法（list, get, create, update, delete）\n3. 使用 ResponseResult 封装响应\n4. 添加 Swagger 注解\n请询问用户需要创建的资源名称和所在模块。"
  }
}
